import React, { useState, useCallback, useEffect } from "react";

// localStorage管理
const LS_USED = "fe_used_ids";
const LS_SESSIONS = "fe_sessions";
const LS_MISSED = "fe_missed";
const LS_LIFETIME = "fe_lifetime"; // 生涯累計(セッション履歴とは別に、間引かれず増え続ける集計)
const LS_CYCLE = "fe_cycle"; // 今回の周回成績(問題プールを1周する間、リロードしても消えない)
const LS_QVERSION = "fe_qversion"; // 問題内容のバージョン識別子(内容が変わったら周回・復習リストを自動リセットするために使う)

const store = {
  saveIds(ids){ try{ localStorage.setItem(LS_USED, JSON.stringify(ids)); }catch(e){} },
  loadIds(){ try{ const v=localStorage.getItem(LS_USED); return v?JSON.parse(v):[]; }catch(e){ return []; } },
  addSession(session){
    try{
      // 生涯累計を先に確定(この時点ではまだ今回のsessionを含まない状態で
      // 初回マイグレーションのベースラインを作る。二重計上を防ぐため必ず
      // sessionsの更新より前に行う)
      const lt = this.loadLifetime();
      lt.totalAnswered += session.total;
      lt.totalCorrect += session.correct;
      Object.entries(session.cats||{}).forEach(([c,v])=>{
        if(!lt.byCat[c]) lt.byCat[c] = {ok:0,total:0};
        lt.byCat[c].ok += v.ok;
        lt.byCat[c].total += v.total;
      });
      this.saveLifetime(lt);

      const sessions = this.loadSessions();
      sessions.push(session);
      // 直近セッション一覧の表示用に最新100件だけ保持(この間引きは表示用のみで、
      // 累計成績はfe_lifetimeに別途加算保存するため間引きの影響を受けない)
      if(sessions.length > 100) sessions.splice(0, sessions.length - 100);
      localStorage.setItem(LS_SESSIONS, JSON.stringify(sessions));
    }catch(e){}
  },
  loadSessions(){ try{ const v=localStorage.getItem(LS_SESSIONS); return v?JSON.parse(v):[]; }catch(e){ return []; } },
  saveLifetime(lt){ try{ localStorage.setItem(LS_LIFETIME, JSON.stringify(lt)); }catch(e){} },
  loadLifetime(){
    try{
      const v = localStorage.getItem(LS_LIFETIME);
      if(v) return JSON.parse(v);
    }catch(e){}
    // 初回: fe_lifetimeが存在しない場合、既存のfe_sessions(直近最大100件、
    // =この呼び出し時点でまだ今回のsessionは含まれていない)の合計値を初期値として
    // 引き継ぐ。これより前に間引かれてしまった分は復元不可能だが、
    // 以後は間引かれずに正しく積み上がっていく。
    const sessions = this.loadSessions();
    const lt = { totalAnswered:0, totalCorrect:0, byCat:{} };
    sessions.forEach(sess=>{
      lt.totalAnswered += sess.total;
      lt.totalCorrect += sess.correct;
      Object.entries(sess.cats||{}).forEach(([c,v])=>{
        if(!lt.byCat[c]) lt.byCat[c] = {ok:0,total:0};
        lt.byCat[c].ok += v.ok;
        lt.byCat[c].total += v.total;
      });
    });
    return lt;
  },
  saveMissed(list){ try{ localStorage.setItem(LS_MISSED, JSON.stringify(list)); }catch(e){} },
  loadMissed(){ try{ const v=localStorage.getItem(LS_MISSED); return v?JSON.parse(v):[]; }catch(e){ return []; } },
  // 今回の周回成績: 問題プールを1周する(usedIdsがリセットされる)まで保持し、
  // ページのリロードやタブの再読込では消えないようにする
  saveCycle(data){ try{ localStorage.setItem(LS_CYCLE, JSON.stringify(data)); }catch(e){} },
  loadCycle(){ try{ const v=localStorage.getItem(LS_CYCLE); return v?JSON.parse(v):{history:[],catStats:{}}; }catch(e){ return {history:[],catStats:{}}; } },
  clearCycle(){ try{ localStorage.removeItem(LS_CYCLE); }catch(e){} },
};

const CATS = [
  "すべて","基礎理論","コンピュータシステム","ネットワーク","情報セキュリティ",
  "データベース","アルゴリズム・プログラミング","ソフトウェア・HI",
  "システム開発","プロジェクトマネジメント","サービスマネジメント・監査","経営・戦略・法務",
];

const ALL_QUESTIONS = [{"id":1,"cat":"ネットワーク","topic":"ネットワークアドレスの算出(新数値)","q":"IPアドレス172.16.45.200、サブネットマスク255.255.255.192のとき、このホストが属するネットワークアドレスはどれか。","choices":[{"label":"ア","text":"172.16.45.224"},{"label":"イ","text":"172.16.45.128"},{"label":"ウ","text":"172.16.45.192"},{"label":"エ","text":"172.16.45.0"}],"correct":"ウ","hint":"255.255.255.192は/26(64個区切り)であり、200は192〜255の区間に属するため、ネットワークアドレスは172.16.45.192となる。"},{"id":2,"cat":"ネットワーク","topic":"ホスト数計算(/26パターン)","q":"プレフィックス長/26のサブネットにおいて、割り当て可能なホストアドレスの最大数はどれか。","choices":[{"label":"ア","text":"64"},{"label":"イ","text":"30"},{"label":"ウ","text":"126"},{"label":"エ","text":"62"}],"correct":"エ","hint":"/26はホスト部6ビットであり、2^6-2=62個のホストアドレスを割り当てられる。"},{"id":3,"cat":"ネットワーク","topic":"ICMPの用途","q":"ネットワーク上の他ホストへの到達可否や応答時間を確認するpingコマンドが利用しているプロトコルはどれか。","choices":[{"label":"ア","text":"ARP"},{"label":"イ","text":"ICMP"},{"label":"ウ","text":"IGMP"},{"label":"エ","text":"SNMP"}],"correct":"イ","hint":"ICMPはエコー要求・応答メッセージ等により、ネットワーク層での疎通確認やエラー通知に使われる。"},{"id":4,"cat":"ネットワーク","topic":"NATの仕組み","q":"社内LANのプライベートIPアドレスを、インターネット通信の際にグローバルIPアドレスへ変換する仕組みはどれか。","choices":[{"label":"ア","text":"DNS"},{"label":"イ","text":"ARP"},{"label":"ウ","text":"NAT(ネットワークアドレス変換)"},{"label":"エ","text":"DHCP"}],"correct":"ウ","hint":"プライベートアドレス空間を外部に直接公開せずにインターネット接続を可能にする代表的な仕組み。"},{"id":5,"cat":"ネットワーク","topic":"DNSラウンドロビンの目的","q":"同一のドメイン名に複数のIPアドレスを登録しておき、問い合わせのたびに異なる順序でIPアドレスを応答することで負荷を分散させる手法はどれか。","choices":[{"label":"ア","text":"ゾーン転送"},{"label":"イ","text":"DNSラウンドロビン"},{"label":"ウ","text":"DNSキャッシュポイズニング(攻撃手法であり異なる)"},{"label":"エ","text":"逆引きDNS"}],"correct":"イ","hint":"簡易な負荷分散手法だが、障害時に異常なサーバのIPも返し続けてしまう欠点がある。"},{"id":6,"cat":"ネットワーク","topic":"VLANの目的","q":"1台の物理スイッチ内を複数の論理的なブロードキャストドメインに分割し、部署ごとに通信を分離する技術はどれか。","choices":[{"label":"ア","text":"スパニングツリー"},{"label":"イ","text":"ポートミラーリング"},{"label":"ウ","text":"VLAN(仮想LAN)"},{"label":"エ","text":"リンクアグリゲーション"}],"correct":"ウ","hint":"物理的な配線構成を変えずに、論理的にネットワークを分割し、セキュリティや管理性を高められる。"},{"id":7,"cat":"ネットワーク","topic":"無線LANのSSIDステルスの限界","q":"無線LANのアクセスポイントでSSIDのブロードキャストを停止(ステルス化)する対策について、セキュリティ上正しい説明はどれか。","choices":[{"label":"ア","text":"これだけで不正アクセスを完全に防止できる"},{"label":"イ","text":"電波の到達距離が短くなる"},{"label":"ウ","text":"暗号化強度が自動的に向上する"},{"label":"エ","text":"通信を解析すればSSIDを特定できるため、単独では十分な対策とはいえない"}],"correct":"エ","hint":"SSIDステルスは目視での発見をやや困難にするだけで、通信を傍受すればSSIDは判別可能であり、暗号化等の対策と併用すべきとされる。"},{"id":8,"cat":"ネットワーク","topic":"QoS(帯域制御)の目的","q":"音声通話やビデオ会議のパケットを他の通信より優先的に転送し、遅延やゆらぎを抑える仕組みはどれか。","choices":[{"label":"ア","text":"スパニングツリー"},{"label":"イ","text":"QoS(サービス品質制御)"},{"label":"ウ","text":"NAPT"},{"label":"エ","text":"ロードバランシング"}],"correct":"イ","hint":"帯域が限られた回線でも、リアルタイム性が求められる通信を優先させることで品質を維持できる。"},{"id":9,"cat":"ネットワーク","topic":"IPv6アドレスの特徴","q":"IPv4のアドレス枯渇問題を解決するために設計されたIPv6のアドレス長はどれか。","choices":[{"label":"ア","text":"32ビット"},{"label":"イ","text":"64ビット"},{"label":"ウ","text":"128ビット"},{"label":"エ","text":"256ビット"}],"correct":"ウ","hint":"IPv4の32ビットに対し、IPv6は128ビットの広大なアドレス空間を持つ。"},{"id":10,"cat":"ネットワーク","topic":"ロードバランサのヘルスチェック","q":"ロードバランサが配下の複数サーバへ定期的にリクエストを送り、応答がないサーバを振り分け対象から自動的に除外する仕組みはどれか。","choices":[{"label":"ア","text":"フェイルオーバー(切替え動作自体を指す別概念)"},{"label":"イ","text":"ラウンドロビン"},{"label":"ウ","text":"スティッキーセッション"},{"label":"エ","text":"ヘルスチェック"}],"correct":"エ","hint":"異常なサーバへのアクセスを未然に防ぎ、サービス全体の可用性を保つために用いられる。"},{"id":11,"cat":"ネットワーク","topic":"CDNの目的","q":"世界各地に配置したキャッシュサーバを利用し、利用者に地理的に近いサーバからコンテンツを配信することで応答速度を向上させる仕組みはどれか。","choices":[{"label":"ア","text":"DMZ"},{"label":"イ","text":"CDN(コンテンツデリバリネットワーク)"},{"label":"ウ","text":"VPN"},{"label":"エ","text":"プロキシサーバ(単一拠点での中継が主目的)"}],"correct":"イ","hint":"オリジンサーバへの負荷集中も同時に軽減できる、大規模Webサービスで広く使われる仕組み。"},{"id":12,"cat":"ネットワーク","topic":"メールプロトコルの役割分担","q":"利用者がメールサーバに残したまま複数端末で同じメールボックスの状態を同期して閲覧できる、メール受信用プロトコルはどれか。","choices":[{"label":"ア","text":"FTP"},{"label":"イ","text":"SMTP(送信用プロトコル)"},{"label":"ウ","text":"POP3(基本的にサーバから削除して取得)"},{"label":"エ","text":"IMAP"}],"correct":"エ","hint":"POP3はメールを端末にダウンロードして管理するのに対し、IMAPはサーバ上でメールを一元管理する。"},{"id":13,"cat":"ネットワーク","topic":"パケットフィルタリング型ファイアウォールの判定基準","q":"パケットフィルタリング型ファイアウォールが通信を許可・拒否する際に主に参照する情報はどれか。","choices":[{"label":"ア","text":"利用者の入力した氏名"},{"label":"イ","text":"画面の解像度"},{"label":"ウ","text":"送信元/宛先IPアドレスやポート番号などのヘッダ情報"},{"label":"エ","text":"ファイルの拡張子のみ"}],"correct":"ウ","hint":"パケットのヘッダ情報とあらかじめ定義したルールを照合し、通過の可否を判定する。"},{"id":14,"cat":"ネットワーク","topic":"伝送時間の計算(新数値)","q":"伝送速度100Mbpsの回線で30MBのファイルをダウンロードするのにかかる理論上の最短時間に最も近いものはどれか(1バイト=8ビット、オーバーヘッドは無視する)。","choices":[{"label":"ア","text":"0.24秒"},{"label":"イ","text":"1.2秒"},{"label":"ウ","text":"2.4秒"},{"label":"エ","text":"24秒"}],"correct":"ウ","hint":"30MB=240Mbitであり、100Mbpsの回線では240÷100=2.4秒で転送できる計算になる。"},{"id":15,"cat":"ネットワーク","topic":"全二重通信の利点","q":"送信と受信を同時に独立して行える通信方式(全二重通信)が、送信と受信を交互にしか行えない方式(半二重通信)に比べて優れている点はどれか。","choices":[{"label":"ア","text":"ケーブルの本数を減らせる"},{"label":"イ","text":"IPアドレスを節約できる"},{"label":"ウ","text":"消費電力が必ず小さくなる"},{"label":"エ","text":"衝突(コリジョン)が発生せず、実効的な通信効率が高い"}],"correct":"エ","hint":"現在の有線LANスイッチ接続では全二重通信が一般的であり、半二重特有の衝突検知処理が不要になる。"},{"id":16,"cat":"アルゴリズム・プログラミング","topic":"クイックソートの最悪計算量","q":"クイックソートにおいて、ピボットの選び方が悪く常に最小値または最大値が選ばれ続けた場合の最悪計算量はどれか。","choices":[{"label":"ア","text":"O(n²)"},{"label":"イ","text":"O(n log n)"},{"label":"ウ","text":"O(log n)"},{"label":"エ","text":"O(n)"}],"correct":"ア","hint":"分割のたびに一方の部分列の要素数がほとんど減らないため、通常の平均計算量O(n log n)より悪化する。"},{"id":17,"cat":"アルゴリズム・プログラミング","topic":"マージソートの計算量の根拠","q":"マージソートが安定してO(n log n)の計算量となる理由として最も適切なものはどれか。","choices":[{"label":"ア","text":"配列を半分に分割する処理をlog n回繰り返し、各段階でn個の要素をマージするため"},{"label":"イ","text":"ピボットを基準に要素を振り分けるため"},{"label":"ウ","text":"常に配列の先頭要素だけを比較するため"},{"label":"エ","text":"要素を1つずつ挿入位置までずらして格納するため"}],"correct":"ア","hint":"分割統治法に基づき、分割の深さがlog n、各深さでのマージ処理がnであるため、全体でO(n log n)となる。"},{"id":18,"cat":"アルゴリズム・プログラミング","topic":"動的計画法(メモ化)の目的","q":"フィボナッチ数列を再帰で計算する際、同じ引数に対する計算結果を配列等に保存しておき、再計算を避ける手法を何と呼ぶか。","choices":[{"label":"ア","text":"インライン展開"},{"label":"イ","text":"メモ化(動的計画法の一種)"},{"label":"ウ","text":"末尾呼び出し最適化"},{"label":"エ","text":"ガベージコレクション"}],"correct":"イ","hint":"単純な再帰では同じ計算が指数的に重複するが、メモ化により計算量を大幅に削減できる。"},{"id":19,"cat":"アルゴリズム・プログラミング","topic":"スタックによる括弧対応チェック","q":"文字列中の丸括弧の対応が正しく取れているかを判定するアルゴリズムで、開き括弧が来たときと閉じ括弧が来たときにそれぞれ行う操作の組合せはどれか。","choices":[{"label":"ア","text":"開き括弧は無視し、閉じ括弧のみカウントする"},{"label":"イ","text":"開き括弧はスタックにpush、閉じ括弧はスタックからpopして対応を確認"},{"label":"ウ","text":"両方とも配列の末尾に追加するだけ"},{"label":"エ","text":"開き括弧はキューにenqueue、閉じ括弧はdequeue"}],"correct":"イ","hint":"最後に開いた括弧と最初に対応させる必要があるため、LIFO(後入れ先出し)構造のスタックが適している。"},{"id":20,"cat":"アルゴリズム・プログラミング","topic":"連結リストと配列の挿入コスト比較","q":"先頭への要素挿入を頻繁に行う場合、配列と単方向連結リストを比較したときの一般的な特徴はどれか。","choices":[{"label":"ア","text":"配列の方が常に高速である"},{"label":"イ","text":"配列も連結リストも挿入コストは常に同じである"},{"label":"ウ","text":"連結リストは要素数に比例したずらし処理が必要になる"},{"label":"エ","text":"配列は既存要素を1つずつ後ろにずらす必要があるが、連結リストは先頭ポインタの付け替えのみで済む"}],"correct":"エ","hint":"配列の先頭挿入はO(n)、連結リストの先頭挿入はO(1)となる典型的な違い。"},{"id":21,"cat":"アルゴリズム・プログラミング","topic":"グラフ表現の使い分け","q":"頂点数は多いが辺の数が少ない疎なグラフを扱う場合、メモリ効率の観点から一般に適したグラフの表現方法はどれか。","choices":[{"label":"ア","text":"隣接リスト"},{"label":"イ","text":"隣接テーブル(標準的な用語ではない)"},{"label":"ウ","text":"隣接行列(頂点数の2乗のメモリを要する)"},{"label":"エ","text":"距離行列"}],"correct":"ア","hint":"隣接行列は頂点数nに対しO(n²)のメモリを要するため、辺が少ない疎グラフでは隣接リストの方が効率的である。"},{"id":22,"cat":"アルゴリズム・プログラミング","topic":"貪欲法の特徴と限界","q":"各段階でその時点における最良の選択を繰り返していくことで解を求めるアルゴリズム設計手法の説明として適切なものはどれか。","choices":[{"label":"ア","text":"動的計画法であり、常に全体最適解が保証される"},{"label":"イ","text":"貪欲法(グリーディ法)であり、必ずしも全体最適解が得られるとは限らない"},{"label":"ウ","text":"バックトラック法であり、全ての組合せを網羅的に試す"},{"label":"エ","text":"分割統治法であり、問題を独立した部分問題に分割する"}],"correct":"イ","hint":"局所的に最良の選択を積み重ねる手法だが、問題によっては大域的な最適解に到達しない場合がある。"},{"id":23,"cat":"アルゴリズム・プログラミング","topic":"インターフェースと抽象クラスの違い","q":"オブジェクト指向言語における「インターフェース」と「抽象クラス」の一般的な違いの説明として適切なものはどれか。","choices":[{"label":"ア","text":"両者に機能的な違いは一切ない"},{"label":"イ","text":"インターフェースは1つのクラスに1つしか実装できない"},{"label":"ウ","text":"抽象クラスはインスタンス化を必ず許可する"},{"label":"エ","text":"インターフェースは実装を持たないメソッドの宣言のみを定義し、抽象クラスは共通の実装(処理内容)も併せて持てる"}],"correct":"エ","hint":"言語によって細部は異なるが、抽象クラスは共通処理の再利用、インターフェースは実装すべき仕様の統一という役割分担が一般的。"},{"id":24,"cat":"アルゴリズム・プログラミング","topic":"カプセル化の目的","q":"オブジェクトの内部データを外部から直接操作できないようにし、決められたメソッドを通じてのみアクセスさせる設計原則を何と呼ぶか。","choices":[{"label":"ア","text":"継承"},{"label":"イ","text":"カプセル化"},{"label":"ウ","text":"多態性(ポリモーフィズムと同義)"},{"label":"エ","text":"ポリモーフィズム"}],"correct":"イ","hint":"内部実装の詳細を隠蔽することで、外部からの不正な状態変更を防ぎ、保守性を高める。"},{"id":25,"cat":"アルゴリズム・プログラミング","topic":"例外処理の目的","q":"プログラム実行中に発生したエラーを、通常の処理フローとは別の経路で捕捉し、適切な後処理を行う仕組みを何と呼ぶか。","choices":[{"label":"ア","text":"デバッグ"},{"label":"イ","text":"例外処理(try-catch等)"},{"label":"ウ","text":"ガベージコレクション"},{"label":"エ","text":"リファクタリング"}],"correct":"イ","hint":"エラー発生時に処理を中断させず、想定した形でエラーへの対応やリソース解放を行える。"},{"id":26,"cat":"アルゴリズム・プログラミング","topic":"二分探索木の中順走査","q":"二分探索木において、左部分木・根・右部分木の順に訪問する走査方法(中順走査)を行った場合に得られる出力の特徴はどれか。","choices":[{"label":"ア","text":"根の要素が必ず最初に出力される"},{"label":"イ","text":"要素が降順に並んだ順序で出力される"},{"label":"ウ","text":"出力順序に一定の規則性はない"},{"label":"エ","text":"要素が昇順に並んだ順序で出力される"}],"correct":"エ","hint":"二分探索木の性質(左<根<右)により、中順走査は自動的にソート済みの順序で要素を返す。"},{"id":27,"cat":"アルゴリズム・プログラミング","topic":"ハッシュ表の衝突対策(チェイン法)","q":"異なるキーが同じハッシュ値になった(衝突が発生した)場合に、同一バケット内で連結リストを使って複数要素を保持する対処法はどれか。","choices":[{"label":"ア","text":"チェイン法(連鎖法)"},{"label":"イ","text":"オープンアドレス法(別バケットを順に探索する方式)"},{"label":"ウ","text":"二分探索法"},{"label":"エ","text":"リハッシュ(バケット数自体を増やす操作)"}],"correct":"ア","hint":"チェイン法は各バケットにリストを持たせ、衝突した要素を同じバケット内に連結して格納する。"},{"id":28,"cat":"アルゴリズム・プログラミング","topic":"計算量オーダーの実務的な違い","q":"データ件数が100万件のとき、計算量O(n²)のアルゴリズムがO(n log n)のアルゴリズムに比べて実行時間の面でどうなるかについて適切な説明はどれか。","choices":[{"label":"ア","text":"件数が多いほど処理時間の差が急激に拡大し、O(n²)は非常に遅くなる"},{"label":"イ","text":"常にO(n²)の方が高速である"},{"label":"ウ","text":"件数が増えても両者の処理時間の差はほぼ一定である"},{"label":"エ","text":"計算量オーダーは実行時間と無関係である"}],"correct":"ア","hint":"O(n²)は件数の2乗に比例して増加するため、大規模データではO(n log n)との差が極めて大きくなる。"},{"id":29,"cat":"アルゴリズム・プログラミング","topic":"単体テストにおけるモックオブジェクトの目的","q":"外部のデータベースやAPIに依存する処理を単体テストする際、それらの代わりに用意した疑似的なオブジェクト(モック)を使うことの主な目的はどれか。","choices":[{"label":"ア","text":"テストの実行を意図的に遅くするため"},{"label":"イ","text":"実際のデータベースへの負荷を意図的に増やすため"},{"label":"ウ","text":"本番環境と全く同じ処理を再現するため"},{"label":"エ","text":"外部依存を排除し、テスト対象のロジックだけを安定して検証できるようにするため"}],"correct":"エ","hint":"外部サービスの状態に左右されず、決定的で再現性のあるテストを行えるようにする代表的な手法。"},{"id":30,"cat":"アルゴリズム・プログラミング","topic":"再帰の停止条件の重要性","q":"再帰関数を実装する際、基底条件(終了条件)を誤って省略した場合に発生しやすい問題はどれか。","choices":[{"label":"ア","text":"処理速度が常に向上する"},{"label":"イ","text":"再帰呼び出しが無限に続き、スタックオーバーフローを引き起こす"},{"label":"ウ","text":"メモリ使用量が常に減少する"},{"label":"エ","text":"コンパイルエラーが必ず発生する"}],"correct":"イ","hint":"基底条件がないと再帰が終了せず、コールスタックが積み上がり続けてスタックオーバーフローに至る。"},{"id":31,"cat":"コンピュータシステム","topic":"クロック周波数と実行時間の計算","q":"クロック周波数2GHzのCPUで、平均CPI(1命令あたりの平均クロックサイクル数)が4の命令列を10億命令実行するのに要する時間に最も近いものはどれか。","choices":[{"label":"ア","text":"0.25秒"},{"label":"イ","text":"0.5秒"},{"label":"ウ","text":"4秒"},{"label":"エ","text":"2秒"}],"correct":"エ","hint":"実行時間=命令数×CPI÷クロック周波数=10億×4÷2×10^9=2秒として求められる。"},{"id":32,"cat":"コンピュータシステム","topic":"パイプラインハザードの理解","q":"パイプライン処理において、ある命令の実行結果を次の命令がすぐに必要とするために発生する処理の遅延を何と呼ぶか。","choices":[{"label":"ア","text":"データハザード"},{"label":"イ","text":"制御ハザード(分岐命令が原因)"},{"label":"ウ","text":"構造ハザード(資源競合が原因)"},{"label":"エ","text":"キャッシュミス"}],"correct":"ア","hint":"前の命令の結果が確定する前に後続命令が実行段階に入ろうとすることで生じる依存関係の問題。"},{"id":33,"cat":"コンピュータシステム","topic":"キャッシュのライトバック方式","q":"CPUがキャッシュ上のデータを更新した際、その変更を主記憶へすぐには反映せず、該当データがキャッシュから追い出されるタイミングでまとめて反映する方式はどれか。","choices":[{"label":"ア","text":"ダイレクトマッピング方式"},{"label":"イ","text":"ライトスルー方式(更新の都度、主記憶にも即座に反映する)"},{"label":"ウ","text":"ライトバック方式"},{"label":"エ","text":"ライトアラウンド方式"}],"correct":"ウ","hint":"主記憶への書き込み回数を減らせるため高速だが、キャッシュと主記憶の一時的な不整合管理が必要になる。"},{"id":34,"cat":"コンピュータシステム","topic":"マルチコアとマルチプロセッサの違い","q":"1つのCPUパッケージ内に複数の演算コアを集積した構成(マルチコア)と、複数の独立したCPUチップを1台のコンピュータに搭載した構成(マルチプロセッサ)の一般的な違いの説明として適切なものはどれか。","choices":[{"label":"ア","text":"マルチコアは必ず並列処理ができない"},{"label":"イ","text":"両者に性能上の違いは一切ない"},{"label":"ウ","text":"マルチコアは同一パッケージ内でキャッシュ等の資源を共有しやすく、チップ間通信のオーバーヘッドが小さい"},{"label":"エ","text":"マルチプロセッサは1つのコアしか持てない"}],"correct":"ウ","hint":"物理的な集積度の違いにより、コア間通信の速度やコスト構造が異なる。"},{"id":35,"cat":"コンピュータシステム","topic":"ハイパーバイザー(仮想化)の役割","q":"1台の物理サーバ上で複数の仮想マシン(ゲストOS)を独立して稼働させるためのソフトウェア基盤を何と呼ぶか。","choices":[{"label":"ア","text":"コンテナランタイム(OSレベル仮想化を担う別方式)"},{"label":"イ","text":"ロードバランサ"},{"label":"ウ","text":"ハイパーバイザー"},{"label":"エ","text":"ファイアウォール"}],"correct":"ウ","hint":"物理リソースを仮想的に分割・割り当てることで、複数のOS環境を1台のハードウェア上に共存させる。"},{"id":36,"cat":"コンピュータシステム","topic":"コンテナ技術と仮想マシンの違い","q":"Dockerのようなコンテナ技術が、従来の仮想マシン(VM)方式と比較して起動が高速でリソース消費が少ないとされる主な理由はどれか。","choices":[{"label":"ア","text":"物理サーバの台数が自動的に増えるため"},{"label":"イ","text":"ディスク容量を必要としないため"},{"label":"ウ","text":"ホストOSのカーネルを共有し、ゲストOS全体を個別に起動する必要がないため"},{"label":"エ","text":"ネットワーク機能を一切使わないため"}],"correct":"ウ","hint":"VMはゲストOSごとカーネルを起動するのに対し、コンテナはホストのカーネルを共有するため軽量になる。"},{"id":37,"cat":"コンピュータシステム","topic":"ファームウェアの役割","q":"ハードウェア機器に組み込まれ、電源投入時の初期化やハードウェア制御を担う、変更頻度の低いソフトウェアを何と呼ぶか。","choices":[{"label":"ア","text":"ファームウェア"},{"label":"イ","text":"デバイスドライバ(OS上で個別機器を制御するソフト)"},{"label":"ウ","text":"ミドルウェア(OSとアプリの中間層を担う別概念)"},{"label":"エ","text":"ハイパーバイザー"}],"correct":"ア","hint":"機器の基本動作を制御する低レベルなソフトウェアであり、必要に応じてアップデートが提供される。"},{"id":38,"cat":"コンピュータシステム","topic":"割込み優先度の処理","q":"複数の割込み要求が同時に発生した場合、CPUがそれらの処理順序を決定するために参照する情報はどれか。","choices":[{"label":"ア","text":"IPアドレス"},{"label":"イ","text":"ファイルサイズ"},{"label":"ウ","text":"キャッシュヒット率"},{"label":"エ","text":"割込み優先度(割込みレベル)"}],"correct":"エ","hint":"優先度の高い割込みから順に処理することで、緊急性の高い処理を適切に扱える。"},{"id":39,"cat":"コンピュータシステム","topic":"ラウンドロビンスケジューリングの特徴","q":"各プロセスに一定時間(タイムクウォンタム)ずつ順番にCPUを割り当てて処理を進めるスケジューリング方式はどれか。","choices":[{"label":"ア","text":"ラウンドロビン方式"},{"label":"イ","text":"優先度順方式(優先度に基づき割当てる別方式)"},{"label":"ウ","text":"到着順方式(FCFS)"},{"label":"エ","text":"最短処理時間順方式"}],"correct":"ア","hint":"各プロセスへ公平にCPU時間を分配でき、対話型システムの応答性向上に適した方式。"},{"id":40,"cat":"コンピュータシステム","topic":"SSD(NANDフラッシュ)の特性","q":"SSDに用いられるNANDフラッシュメモリが、同じセルへの書き込みを繰り返すと劣化していく特性に対応するために行われる、書き込み箇所を分散させる制御はどれか。","choices":[{"label":"ア","text":"ウェアレベリング"},{"label":"イ","text":"キャッシュフラッシュ"},{"label":"ウ","text":"ジャーナリング"},{"label":"エ","text":"デフラグメンテーション(HDD向けの最適化手法)"}],"correct":"ア","hint":"特定のセルだけに書き込みが集中して寿命が縮まらないよう、書き込み先を均等に分散させる仕組み。"},{"id":41,"cat":"コンピュータシステム","topic":"バスの種類の理解","q":"CPUが主記憶上の読み書き対象の位置(番地)を指定するために信号を送る経路を何と呼ぶか。","choices":[{"label":"ア","text":"アドレスバス"},{"label":"イ","text":"制御バス(制御信号をやり取りする経路)"},{"label":"ウ","text":"データバス(実際のデータ本体をやり取りする経路)"},{"label":"エ","text":"電源バス"}],"correct":"ア","hint":"アドレスバスで番地を指定し、データバスで実際の値を転送し、制御バスで読み書きのタイミング等を制御する。"},{"id":42,"cat":"システム開発","topic":"スプリントレビューの目的","q":"スクラム開発において、スプリントの終わりに完成した成果物をステークホルダーに披露し、フィードバックを得る場を何と呼ぶか。","choices":[{"label":"ア","text":"レトロスペクティブ(チーム内の振り返り)"},{"label":"イ","text":"スプリントプランニング(着手前の計画)"},{"label":"ウ","text":"スプリントレビュー"},{"label":"エ","text":"デイリースクラム(毎日の短い進捗共有)"}],"correct":"ウ","hint":"成果物そのものに対するフィードバックを得る場であり、チーム内の進め方を振り返るレトロスペクティブとは目的が異なる。"},{"id":43,"cat":"システム開発","topic":"モジュールの結合度と凝集度","q":"モジュール設計における「結合度」と「凝集度」の望ましい状態の組合せはどれか。","choices":[{"label":"ア","text":"結合度も凝集度もどちらも高い方がよい"},{"label":"イ","text":"結合度は高く、凝集度は低いことが望ましい"},{"label":"ウ","text":"結合度は低く、凝集度は高いことが望ましい"},{"label":"エ","text":"結合度も凝集度もどちらも低い方がよい"}],"correct":"ウ","hint":"モジュール間の依存を減らし(低結合)、モジュール内の関連する処理をまとめる(高凝集)ことで保守性が高まる。"},{"id":44,"cat":"システム開発","topic":"リグレッションテストの目的","q":"既存の機能に対して修正を加えた際、その修正が意図せず他の既存機能に悪影響を与えていないかを確認するテストを何と呼ぶか。","choices":[{"label":"ア","text":"負荷テスト"},{"label":"イ","text":"探索的テスト"},{"label":"ウ","text":"リグレッションテスト(回帰テスト)"},{"label":"エ","text":"受入テスト"}],"correct":"ウ","hint":"小さな変更であっても既存の動作を壊していないかを機械的に再確認する、継続的な品質保証の要となるテスト。"},{"id":45,"cat":"システム開発","topic":"プルリクエストによるコードレビューの流れ","q":"Gitを用いた開発で、変更内容をレビュアーに確認してもらってからメインブランチへ統合する一連の仕組みを一般に何と呼ぶか。","choices":[{"label":"ア","text":"プルリクエスト(マージリクエスト)"},{"label":"イ","text":"クローン(リポジトリの複製取得)"},{"label":"ウ","text":"フェッチ(リモートの変更取得)"},{"label":"エ","text":"コミット(変更を記録する単位の操作)"}],"correct":"ア","hint":"変更差分をレビュアーが確認し、承認を経てからマージすることで、コード品質を担保する仕組み。"},{"id":46,"cat":"システム開発","topic":"フィーチャーフラグの活用目的","q":"未完成の新機能をコードに含めたまま本番環境にデプロイしつつ、設定の切り替えだけで機能の有効・無効を制御できるようにする仕組みを何と呼ぶか。","choices":[{"label":"ア","text":"カナリアリリース(段階的な公開手法の一種)"},{"label":"イ","text":"ロールバック(デプロイを取り消す操作)"},{"label":"ウ","text":"ホットフィックス(緊急修正)"},{"label":"エ","text":"フィーチャーフラグ(機能トグル)"}],"correct":"エ","hint":"コードのデプロイと機能の公開を分離できるため、リリースタイミングを柔軟に制御できる。"},{"id":47,"cat":"システム開発","topic":"ブルーグリーンデプロイメントの仕組み","q":"現行環境(ブルー)とは別に新バージョンの環境(グリーン)を並行して用意し、切替えを一瞬で行うことでダウンタイムを最小化するデプロイ手法はどれか。","choices":[{"label":"ア","text":"ブルーグリーンデプロイメント"},{"label":"イ","text":"カナリアリリース(一部利用者から段階的に展開する手法)"},{"label":"ウ","text":"ビッグバンリリース"},{"label":"エ","text":"ローリングアップデート(順次入れ替える手法)"}],"correct":"ア","hint":"問題が発生した場合も、古い環境(ブルー)に即座に戻せる点が大きな利点となる。"},{"id":48,"cat":"システム開発","topic":"レトロスペクティブの目的","q":"スプリントの終了後にチーム内で開発プロセス自体を振り返り、次のスプリントに向けた改善点を話し合う場を何と呼ぶか。","choices":[{"label":"ア","text":"デイリースクラム"},{"label":"イ","text":"レトロスペクティブ(ふりかえり)"},{"label":"ウ","text":"プロダクトバックログリファインメント"},{"label":"エ","text":"スプリントレビュー(成果物に対するステークホルダーへの披露)"}],"correct":"イ","hint":"「何がうまくいったか」「何を改善すべきか」をチーム自身で議論し、継続的なプロセス改善につなげる。"},{"id":49,"cat":"システム開発","topic":"ユーザーストーリーの構成要素","q":"アジャイル開発で用いられるユーザーストーリーが一般的に含む要素の組合せとして適切なものはどれか。","choices":[{"label":"ア","text":"誰が(役割)、何を実現したいか(要求)、なぜそれが必要か(理由)"},{"label":"イ","text":"使用するプログラミング言語とデータベースの種類のみ"},{"label":"ウ","text":"開発チームの人数と勤務時間のみ"},{"label":"エ","text":"サーバのIPアドレスとポート番号のみ"}],"correct":"ア","hint":"「〜として、〜したい、なぜなら〜」という形式で、利用者視点の価値を簡潔に表現する。"},{"id":50,"cat":"システム開発","topic":"技術的負債の概念","q":"短期的な納期優先で設計や実装の質を妥協した結果、将来の保守や機能追加のコストが増大していく状態を何と呼ぶか。","choices":[{"label":"ア","text":"技術的負債"},{"label":"イ","text":"レガシーシステム(古くなったシステム全般を指す、より広い概念)"},{"label":"ウ","text":"デッドコード(使われていないコード)"},{"label":"エ","text":"スパゲッティコード(技術的負債の一因となる状態の一例)"}],"correct":"ア","hint":"借金のように、後になるほど「利子」(保守コストの増大)がかさんでいくという比喩から名付けられている。"},{"id":51,"cat":"基礎理論","topic":"16進数から10進数への変換(新数値)","q":"16進数2Fが表す10進数の値はどれか。","choices":[{"label":"ア","text":"63"},{"label":"イ","text":"31"},{"label":"ウ","text":"25"},{"label":"エ","text":"47"}],"correct":"エ","hint":"2F=2×16+15=32+15=47として求められる。"},{"id":52,"cat":"基礎理論","topic":"ド・モルガンの法則","q":"論理式 NOT(A AND B) と等価な式はどれか。","choices":[{"label":"ア","text":"(NOT A) AND (NOT B)"},{"label":"イ","text":"A OR B"},{"label":"ウ","text":"A AND B"},{"label":"エ","text":"(NOT A) OR (NOT B)"}],"correct":"エ","hint":"ド・モルガンの法則により、ANDの否定はそれぞれの否定のORに変換できる。"},{"id":53,"cat":"基礎理論","topic":"丸め誤差の理解","q":"10進小数を有限桁の2進浮動小数点数で近似的にしか表現できないことにより生じる誤差を何と呼ぶか。","choices":[{"label":"ア","text":"情報落ち(絶対値の差が大きい加算で生じる誤差)"},{"label":"イ","text":"丸め誤差"},{"label":"ウ","text":"桁落ち(近い値同士の減算で生じる誤差)"},{"label":"エ","text":"オーバーフロー(桁あふれによる誤差)"}],"correct":"イ","hint":"例えば0.1のような値は2進数で正確に表現できず、格納時点でわずかな誤差が生じる。"},{"id":54,"cat":"基礎理論","topic":"オイラー路が存在する条件","q":"グラフの全ての辺をちょうど1回ずつ通って一筆書きできる(オイラー路が存在する)ための必要条件はどれか。","choices":[{"label":"ア","text":"頂点数が偶数でなければならない"},{"label":"イ","text":"辺の数が頂点数より少ない"},{"label":"ウ","text":"すべての頂点の次数が偶数でなければならない(オイラー閉路の条件)"},{"label":"エ","text":"次数が奇数の頂点の数が0個または2個である"}],"correct":"エ","hint":"奇数次数の頂点が0個ならオイラー閉路(始点に戻る一筆書き)、2個ならその2点を起点・終点とするオイラー路が存在する。"},{"id":55,"cat":"基礎理論","topic":"論理右シフト演算の理解","q":"8ビットの符号なし整数00010110を論理右シフトで2ビットシフトした結果はどれか。","choices":[{"label":"ア","text":"01000000"},{"label":"イ","text":"00000010"},{"label":"ウ","text":"01011000"},{"label":"エ","text":"00000101"}],"correct":"エ","hint":"論理右シフトは各ビットを右へずらし、空いた上位ビットには0を補う。00010110を2ビット右にずらすと00000101になる。"},{"id":56,"cat":"基礎理論","topic":"正規表現の基本パターン","q":"正規表現において、直前の文字が0回以上繰り返すことを表す記号はどれか。","choices":[{"label":"ア","text":"?(クエスチョン、0回または1回を表す)"},{"label":"イ","text":"^(先頭一致を表す)"},{"label":"ウ","text":"+(プラス、1回以上の繰返しを表す)"},{"label":"エ","text":"*(アスタリスク)"}],"correct":"エ","hint":"*は直前の要素の0回以上の繰返しにマッチし、空文字列にもマッチしうる点が+との違いである。"},{"id":57,"cat":"基礎理論","topic":"スタックとキューの適用場面比較","q":"ブラウザの「戻る」ボタンの履歴管理と、印刷ジョブの処理順管理に、それぞれ適したデータ構造の組合せはどれか。","choices":[{"label":"ア","text":"両方ともスタックが適する"},{"label":"イ","text":"戻るボタン:キュー、印刷ジョブ:スタック"},{"label":"ウ","text":"両方ともキューが適する"},{"label":"エ","text":"戻るボタン:スタック、印刷ジョブ:キュー"}],"correct":"エ","hint":"戻るボタンは直近の履歴から遡るLIFO(スタック)、印刷ジョブは投入順に処理するFIFO(キュー)が適する。"},{"id":58,"cat":"基礎理論","topic":"有限オートマトンの基礎","q":"現在の状態と入力記号の組合せによって、次に遷移する状態が一意に定まるオートマトンを何と呼ぶか。","choices":[{"label":"ア","text":"決定性有限オートマトン(DFA)"},{"label":"イ","text":"プッシュダウンオートマトン"},{"label":"ウ","text":"チューリングマシン(より計算能力の高いモデル)"},{"label":"エ","text":"非決定性有限オートマトン(NFA、複数の遷移先が存在しうる)"}],"correct":"ア","hint":"DFAでは各状態・入力の組に対して遷移先が高々1つに定まるため、動作の予測や実装がしやすい。"},{"id":59,"cat":"基礎理論","topic":"ビット演算による特定ビットの判定","q":"変数xの下位から3ビット目(0始まりでビット2)が1かどうかを判定したい場合に用いる演算はどれか。","choices":[{"label":"ア","text":"x AND 00000100(2進数)の結果が0でないかを確認する"},{"label":"イ","text":"x OR 11111111の結果を確認する"},{"label":"ウ","text":"x NOT演算の結果のみを確認する"},{"label":"エ","text":"x XOR 00000000の結果を確認する"}],"correct":"ア","hint":"調べたいビット位置のみを1にしたマスク値とAND演算を取ることで、そのビットが1かどうかを判定できる。"},{"id":60,"cat":"データベース","topic":"外部キー制約の目的","q":"子テーブルの列が親テーブルに存在しない値を参照することを防ぐために設定する制約はどれか。","choices":[{"label":"ア","text":"主キー制約(一意性と非NULLを保証する制約)"},{"label":"イ","text":"CHECK制約(値の範囲等を検証する制約)"},{"label":"ウ","text":"外部キー制約(参照整合性制約)"},{"label":"エ","text":"UNIQUE制約"}],"correct":"ウ","hint":"親テーブルに存在しない値の挿入や、参照されている親レコードの不用意な削除を防ぎ、データの整合性を保つ。"},{"id":61,"cat":"データベース","topic":"トリガーの活用場面","q":"特定のテーブルに対してINSERTやUPDATEが実行された際に、データベース側で自動的に付随する処理(監査ログの記録等)を実行させる仕組みはどれか。","choices":[{"label":"ア","text":"トリガー"},{"label":"イ","text":"ビュー(仮想的な表)"},{"label":"ウ","text":"ストアドプロシージャ(明示的に呼び出して実行する処理)"},{"label":"エ","text":"インデックス"}],"correct":"ア","hint":"特定のイベントをきっかけに自動的に起動する点が、明示的に呼び出すストアドプロシージャとの違い。"},{"id":62,"cat":"データベース","topic":"ビューの利点","q":"複雑なJOINを含むSELECT文をあらかじめ定義しておき、単純なテーブルのように参照できるようにする仕組み(ビュー)の利点はどれか。","choices":[{"label":"ア","text":"元のテーブルのデータを自動的に暗号化する"},{"label":"イ","text":"複雑なクエリを隠蔽し、利用者は単純な参照だけで済むようになる"},{"label":"ウ","text":"実データを物理的に複製して保存するため検索が必ず高速になる"},{"label":"エ","text":"トランザクション管理が不要になる"}],"correct":"イ","hint":"ビューは仮想的な表であり、実体データを持たず、定義されたクエリを都度実行して結果を返す(マテリアライズドビューは例外)。"},{"id":63,"cat":"データベース","topic":"排他ロックと共有ロックの違い","q":"複数のトランザクションが同じデータに対して同時に取得できるロックと、1つのトランザクションしか取得できないロックの組合せとして適切なものはどれか。","choices":[{"label":"ア","text":"両方とも複数同時取得できる"},{"label":"イ","text":"両方とも単独のトランザクションしか取得できない"},{"label":"ウ","text":"共有ロック(読取り用)は複数同時取得可、排他ロック(書込み用)は単独のみ"},{"label":"エ","text":"排他ロックは複数同時取得可、共有ロックは単独のみ"}],"correct":"ウ","hint":"読取り同士は競合しないため共有ロックは同時取得を許すが、更新を伴う排他ロックは1つに限定してデータ不整合を防ぐ。"},{"id":64,"cat":"データベース","topic":"NoSQLとRDBMSの使い分け","q":"スキーマ(表構造)が頻繁に変化し、大量の非定型データを柔軟に格納したい場合に、一般的にRDBMSより適しているとされるデータベースの分類はどれか。","choices":[{"label":"ア","text":"正規化されたRDBMS"},{"label":"イ","text":"NoSQL(ドキュメント指向データベース等)"},{"label":"ウ","text":"OLTP(取引処理に特化した利用形態を指す概念)"},{"label":"エ","text":"ER図(設計手法の一種)"}],"correct":"イ","hint":"厳密なスキーマ定義を必要としない柔軟なデータモデルにより、変化の速いデータ構造に対応しやすい。"},{"id":65,"cat":"データベース","topic":"インデックスの利点と欠点","q":"テーブルに検索用のインデックスを追加した場合に一般的に生じるトレードオフの説明として適切なものはどれか。","choices":[{"label":"ア","text":"検索も更新も両方とも必ず低速化する"},{"label":"イ","text":"検索も更新も両方とも必ず高速化する"},{"label":"ウ","text":"インデックスはディスク容量を一切消費しない"},{"label":"エ","text":"検索(SELECT)は高速化するが、更新(INSERT/UPDATE/DELETE)時にインデックスも更新するためオーバーヘッドが増える"}],"correct":"エ","hint":"インデックスはデータの物理配置とは別に検索用の構造を保持するため、更新のたびに維持コストが発生する。"},{"id":66,"cat":"データベース","topic":"GROUP BYとHAVINGの使い分け","q":"「部署ごとの平均給与を集計し、その中で平均給与が50万円を超える部署だけを表示したい」というSQLを書く際に必要な組合せはどれか。","choices":[{"label":"ア","text":"ORDER BYで集計結果を絞り込む"},{"label":"イ","text":"GROUP BYで部署ごとに集計し、HAVINGで集計後の条件を絞り込む"},{"label":"ウ","text":"GROUP BYのみで条件の絞り込みは不要"},{"label":"エ","text":"WHEREのみで集計後の条件も絞り込む"}],"correct":"イ","hint":"WHEREは集計前の行の絞り込み、HAVINGは集計(GROUP BY)後の結果に対する絞り込みに使う。"},{"id":67,"cat":"データベース","topic":"バックアップ方式の違い","q":"毎回全データをバックアップするのではなく、前回のフルバックアップ以降に変更されたデータだけを都度バックアップする方式を何と呼ぶか。","choices":[{"label":"ア","text":"フルバックアップ(毎回全データを取得する方式)"},{"label":"イ","text":"差分バックアップ"},{"label":"ウ","text":"ミラーリング"},{"label":"エ","text":"増分バックアップ(前回の任意のバックアップ以降の変更分のみを取得する方式)"}],"correct":"イ","hint":"差分バックアップは復元時にフルバックアップと最新の差分の2つを適用すればよく、増分バックアップより復元が単純になる。"},{"id":68,"cat":"経営・戦略・法務","topic":"SWOT分析の要素","q":"自社の強み・弱みという内部要因と、機会・脅威という外部要因を整理して戦略立案に用いるフレームワークはどれか。","choices":[{"label":"ア","text":"SWOT分析"},{"label":"イ","text":"PPM(製品ポートフォリオマネジメント)"},{"label":"ウ","text":"バリューチェーン分析"},{"label":"エ","text":"ファイブフォース分析(業界の競争要因分析)"}],"correct":"ア","hint":"Strengths(強み)・Weaknesses(弱み)・Opportunities(機会)・Threats(脅威)の頭文字から名付けられている。"},{"id":69,"cat":"経営・戦略・法務","topic":"BSC(バランススコアカード)の視点","q":"財務の視点だけでなく、顧客・業務プロセス・学習と成長という複数の視点から企業活動をバランスよく評価する経営管理手法はどれか。","choices":[{"label":"ア","text":"SWOT分析"},{"label":"イ","text":"BSC(バランススコアカード)"},{"label":"ウ","text":"PDCAサイクル"},{"label":"エ","text":"KPI(個々の目標達成度を測る指標そのものを指す概念)"}],"correct":"イ","hint":"財務指標だけに偏らず、中長期的な企業価値向上につながる複数の視点で戦略の実行状況を評価する。"},{"id":70,"cat":"経営・戦略・法務","topic":"個人情報保護法の基本的義務","q":"個人情報取扱事業者が個人データを第三者に提供する場合、原則として必要となる対応はどれか。","choices":[{"label":"ア","text":"常に無条件で提供してよい"},{"label":"イ","text":"本人からあらかじめ同意を得ること"},{"label":"ウ","text":"提供後に本人へ通知すれば同意は不要である"},{"label":"エ","text":"提供先に一切の説明義務はない"}],"correct":"イ","hint":"個人情報保護法では、一定の例外を除き、第三者提供には本人の同意取得が原則として求められる。"},{"id":71,"cat":"経営・戦略・法務","topic":"PPM(プロダクトポートフォリオマネジメント)","q":"市場成長率と市場占有率(シェア)の2軸で自社の事業や製品を分類し、資源配分の優先度を検討するフレームワークはどれか。","choices":[{"label":"ア","text":"ファイブフォース分析"},{"label":"イ","text":"バリューチェーン分析"},{"label":"ウ","text":"PPM(プロダクトポートフォリオマネジメント)"},{"label":"エ","text":"SWOT分析"}],"correct":"ウ","hint":"「花形」「金のなる木」「問題児」「負け犬」の4象限に分類し、資源配分の意思決定に活用する。"},{"id":72,"cat":"経営・戦略・法務","topic":"アライアンス(業務提携)の目的","q":"自社単独では困難な事業展開を、他社と資本関係を持たずに協力関係を築くことで実現しようとする戦略はどれか。","choices":[{"label":"ア","text":"子会社化"},{"label":"イ","text":"垂直統合(自社内で工程を内製化する戦略)"},{"label":"ウ","text":"アライアンス(業務提携)"},{"label":"エ","text":"M&A(企業の合併・買収を伴う戦略)"}],"correct":"ウ","hint":"資本の移動を伴わずに、互いの強みを活かして協力する柔軟な戦略的関係を指す。"},{"id":73,"cat":"経営・戦略・法務","topic":"コンプライアンス違反のリスク","q":"企業が法令や社内規程を遵守せず不正会計等を行った場合に、一般に生じるリスクの説明として適切なものはどれか。","choices":[{"label":"ア","text":"短期的な株価上昇が確実に見込める"},{"label":"イ","text":"行政処分や刑事罰に加え、社会的信用の失墜による事業継続への深刻な悪影響が生じうる"},{"label":"ウ","text":"取引先からの信頼が自動的に高まる"},{"label":"エ","text":"従業員のモチベーションが必ず向上する"}],"correct":"イ","hint":"コンプライアンス違反は法的リスクのみならず、レピュテーション(評判)リスクとして事業全体に波及しうる。"},{"id":74,"cat":"経営・戦略・法務","topic":"電子帳簿保存法の概要","q":"国税関係の帳簿書類を、紙ではなく電磁的記録(電子データ)で保存する際の要件等を定めた法律はどれか。","choices":[{"label":"ア","text":"特定商取引法"},{"label":"イ","text":"不正競争防止法"},{"label":"ウ","text":"下請代金支払遅延等防止法(下請取引の公正化を目的とする法律)"},{"label":"エ","text":"電子帳簿保存法"}],"correct":"エ","hint":"電子データでの保存に関する真実性・可視性の確保等の要件を定めており、近年の改正で対象が拡大している。"},{"id":75,"cat":"経営・戦略・法務","topic":"サブスクリプションモデルの特徴","q":"製品やサービスを都度購入するのではなく、一定期間の利用権に対して継続的に対価を支払う課金モデルを何と呼ぶか。","choices":[{"label":"ア","text":"買い切りモデル"},{"label":"イ","text":"フリーミアムモデル(基本無料・一部有料という別のモデル)"},{"label":"ウ","text":"サブスクリプションモデル"},{"label":"エ","text":"従量課金モデル(使用量に応じて課金する別のモデル)"}],"correct":"ウ","hint":"継続的な収益(ストック収益)を得やすい一方、利用者に継続的な価値提供を続ける必要がある。"},{"id":76,"cat":"サービスマネジメント・監査","topic":"SLAの構成要素","q":"サービス提供者と利用者の間で、稼働率や応答時間などのサービス品質の目標値を明文化して合意する文書はどれか。","choices":[{"label":"ア","text":"SLA(サービスレベルアグリーメント)"},{"label":"イ","text":"RFC(変更要求書)"},{"label":"ウ","text":"SLM(SLAを管理・運用するプロセス自体を指す、より広い概念)"},{"label":"エ","text":"インシデント報告書"}],"correct":"ア","hint":"合意した品質目標を基準に、実績を定期的に評価・改善していくための基礎となる文書。"},{"id":77,"cat":"サービスマネジメント・監査","topic":"インシデント管理と問題管理の違い","q":"「発生した障害を可能な限り早く復旧させること」と、「その障害の根本原因を突き止め再発を防止すること」を担うプロセスの組合せはどれか。","choices":[{"label":"ア","text":"問題管理 — インシデント管理"},{"label":"イ","text":"構成管理 — 問題管理"},{"label":"ウ","text":"インシデント管理 — 問題管理"},{"label":"エ","text":"変更管理 — リリース管理"}],"correct":"ウ","hint":"インシデント管理は迅速な復旧(暫定対応)を、問題管理は根本原因の特定と恒久対策を目的とする。"},{"id":78,"cat":"サービスマネジメント・監査","topic":"IT全般統制の位置づけ","q":"アクセス権限の管理やシステム変更管理など、個々の業務処理ではなくIT基盤全体に共通して整備すべき内部統制を何と呼ぶか。","choices":[{"label":"ア","text":"是正的統制"},{"label":"イ","text":"IT業務処理統制(個々の業務アプリケーションにおける統制)"},{"label":"ウ","text":"予防的統制(統制の分類軸の一つであり別概念)"},{"label":"エ","text":"IT全般統制"}],"correct":"エ","hint":"IT全般統制が有効に機能して初めて、個々のIT業務処理統制の信頼性も担保されるという関係にある。"},{"id":79,"cat":"サービスマネジメント・監査","topic":"RFC(変更要求)の役割","q":"システムへの変更を実施する前に、その内容・影響範囲・実施計画等を記載して申請する文書はどれか。","choices":[{"label":"ア","text":"インシデントチケット"},{"label":"イ","text":"RFC(変更要求書)"},{"label":"ウ","text":"構成管理データベース(CMDB)"},{"label":"エ","text":"SLA"}],"correct":"イ","hint":"変更管理プロセスにおいて、CAB(変更諮問委員会)等での審議の基礎資料となる。"},{"id":80,"cat":"サービスマネジメント・監査","topic":"可用性管理の指標(MTBF/MTTR)","q":"システムの信頼性を評価する際、故障の平均発生間隔を示す指標と、故障からの平均復旧時間を示す指標の組合せはどれか。","choices":[{"label":"ア","text":"MTTR — MTBF(順序が逆)"},{"label":"イ","text":"MTBF(平均故障間隔) — MTTR(平均修理時間)"},{"label":"ウ","text":"SLA — KPI"},{"label":"エ","text":"RTO — RPO(事業継続計画における目標復旧時間・目標復旧時点)"}],"correct":"イ","hint":"MTBFが長く、MTTRが短いほど、可用性(稼働率)の高いシステムであるといえる。"},{"id":81,"cat":"サービスマネジメント・監査","topic":"監査の客観性確保","q":"システム監査を実施する監査人が、監査対象の業務に自ら関与していないことが求められる原則を何と呼ぶか。","choices":[{"label":"ア","text":"独立性の原則"},{"label":"イ","text":"守秘義務の原則(情報の秘密保持に関する原則)"},{"label":"ウ","text":"専門的能力の原則"},{"label":"エ","text":"誠実性の原則"}],"correct":"ア","hint":"監査対象の業務に利害関係を持たないことで、公正かつ客観的な評価が可能になる。"},{"id":82,"cat":"サービスマネジメント・監査","topic":"サービスデスクの単一窓口の利点","q":"利用者からの問合せや障害報告を一元的に受け付ける単一の窓口(SPOC)を設置することの利点はどれか。","choices":[{"label":"ア","text":"対応するスタッフの数を必ず減らせる"},{"label":"イ","text":"システムの処理性能が自動的に向上する"},{"label":"ウ","text":"障害の発生自体を未然に防止できる"},{"label":"エ","text":"利用者が問合せ先に迷わず、対応状況の一元管理がしやすくなる"}],"correct":"エ","hint":"単一窓口(Single Point of Contact)により、利用者の利便性向上と、問合せ状況の一元的な把握・管理が可能になる。"},{"id":83,"cat":"プロジェクトマネジメント","topic":"クリティカルパスの特定","q":"プロジェクトの全工程の中で、遅延がそのままプロジェクト全体の完了遅延に直結する、最も所要日数が長い経路を何と呼ぶか。","choices":[{"label":"ア","text":"フロート(余裕日数を持つ経路上の作業)"},{"label":"イ","text":"クリティカルパス"},{"label":"ウ","text":"マイルストーン(特定時点の節目を示す点)"},{"label":"エ","text":"WBS"}],"correct":"イ","hint":"クリティカルパス上の作業は余裕(フロート)が0であり、1日でも遅れるとプロジェクト全体が遅延する。"},{"id":84,"cat":"プロジェクトマネジメント","topic":"プロジェクト憲章の目的","q":"プロジェクトの目的・目標・主要な制約条件・プロジェクトマネージャーの権限などを定め、プロジェクトの正式な発足を承認する文書はどれか。","choices":[{"label":"ア","text":"リスク登録簿"},{"label":"イ","text":"WBS(作業を分解して管理する成果物)"},{"label":"ウ","text":"プロジェクト憲章"},{"label":"エ","text":"ステータスレポート"}],"correct":"ウ","hint":"プロジェクト憲章の承認により、プロジェクトマネージャーは組織資源を活用する正式な権限を得る。"},{"id":85,"cat":"プロジェクトマネジメント","topic":"ガントチャートの活用","q":"各作業の開始日・終了日・進捗状況を横棒グラフで時系列に表示し、スケジュール全体を視覚的に把握できるようにした図はどれか。","choices":[{"label":"ア","text":"散布図"},{"label":"イ","text":"パレート図(要因の影響度を分析する図)"},{"label":"ウ","text":"ガントチャート"},{"label":"エ","text":"特性要因図(原因を体系的に整理する図)"}],"correct":"ウ","hint":"作業間の順序関係や進捗の遅れを直感的に把握しやすく、スケジュール管理で広く使われる。"},{"id":86,"cat":"プロジェクトマネジメント","topic":"ステークホルダー登録簿の内容","q":"プロジェクトに関与する利害関係者の氏名・役割・関心事項・影響力などをまとめて記録しておく文書はどれか。","choices":[{"label":"ア","text":"課題ログ"},{"label":"イ","text":"ステークホルダー登録簿"},{"label":"ウ","text":"リスク登録簿(リスクに関する情報を記録する文書)"},{"label":"エ","text":"品質マネジメント計画書"}],"correct":"イ","hint":"誰が、どのような立場・関心でプロジェクトに関わるかを可視化し、適切なコミュニケーション戦略の検討に役立てる。"},{"id":87,"cat":"プロジェクトマネジメント","topic":"リスク登録簿の役割","q":"識別されたリスクの内容・発生確率・影響度・対応策などを一覧化して管理する文書はどれか。","choices":[{"label":"ア","text":"変更管理台帳"},{"label":"イ","text":"課題ログ(既に顕在化した課題を記録する文書)"},{"label":"ウ","text":"リスク登録簿"},{"label":"エ","text":"ステークホルダー登録簿"}],"correct":"ウ","hint":"プロジェクト全体を通じて継続的に更新され、リスクへの計画的な対応を支える基礎資料となる。"},{"id":88,"cat":"プロジェクトマネジメント","topic":"品質保証と品質管理の違い","q":"「プロセスが適切に実行されているかを検証する活動」と、「成果物そのものが基準を満たしているかを検証する活動」の組合せとして適切なものはどれか。","choices":[{"label":"ア","text":"リスク管理 — 品質保証"},{"label":"イ","text":"品質管理(QC) — 品質保証(QA)(順序が逆)"},{"label":"ウ","text":"構成管理 — 品質管理"},{"label":"エ","text":"品質保証(QA) — 品質管理(QC)"}],"correct":"エ","hint":"品質保証はプロセス全体の適切性を担保する活動、品質管理は成果物の検査・欠陥検出を行う活動とされる。"},{"id":89,"cat":"プロジェクトマネジメント","topic":"EAC(完成時総コスト見積り)の計算","q":"予算(BAC)が1000万円、コスト効率指数(CPI)が0.8のプロジェクトにおいて、現状の効率がこのまま続くと仮定した場合の完成時総コスト見積り(EAC)はどれか。","choices":[{"label":"ア","text":"1250万円"},{"label":"イ","text":"1200万円"},{"label":"ウ","text":"1000万円"},{"label":"エ","text":"800万円"}],"correct":"ア","hint":"EAC=BAC÷CPI=1000万円÷0.8=1250万円として求められる。"},{"id":90,"cat":"ソフトウェア・HI","topic":"ユーザビリティテストの目的","q":"実際の利用者に製品やサービスを操作してもらい、使いにくい箇所や理解しにくい箇所を発見する評価手法を何と呼ぶか。","choices":[{"label":"ア","text":"負荷テスト(性能面を検証するテスト)"},{"label":"イ","text":"リグレッションテスト"},{"label":"ウ","text":"ユーザビリティテスト"},{"label":"エ","text":"単体テスト"}],"correct":"ウ","hint":"開発者の想定と実際の利用者の行動のずれを早期に発見し、設計へフィードバックできる。"},{"id":91,"cat":"ソフトウェア・HI","topic":"色覚多様性へのアクセシビリティ配慮","q":"赤と緑の識別が困難な色覚特性を持つ利用者にも情報が正しく伝わるようにする配慮として適切なものはどれか。","choices":[{"label":"ア","text":"赤と緑の2色のみを使って情報を強調する"},{"label":"イ","text":"色の違いだけで全ての情報を区別できるようにする"},{"label":"ウ","text":"文字情報を一切使わず色のみで表現する"},{"label":"エ","text":"色の違いだけでなく、形状やアイコン、ラベルなど色以外の手段でも情報を区別できるようにする"}],"correct":"エ","hint":"色だけに依存した情報伝達は一部の利用者にとって認識できない場合があるため、複数の手段を併用することが推奨される。"},{"id":92,"cat":"ソフトウェア・HI","topic":"レスポンシブデザインの目的","q":"PC・タブレット・スマートフォンなど画面サイズの異なる端末に対して、同一のWebページが自動的にレイアウトを最適化する設計手法を何と呼ぶか。","choices":[{"label":"ア","text":"フラットデザイン(視覚デザインの様式を指す概念)"},{"label":"イ","text":"レスポンシブデザイン"},{"label":"ウ","text":"マテリアルデザイン"},{"label":"エ","text":"プログレッシブエンハンスメント(機能を段階的に拡張する別の設計思想)"}],"correct":"イ","hint":"画面幅などに応じてCSSのレイアウトを切り替え、端末ごとに個別のサイトを用意せずに対応できる。"},{"id":93,"cat":"ソフトウェア・HI","topic":"マイクロサービスとモノリシックの違い","q":"1つの巨大なアプリケーションとして構築する方式(モノリシック)と、機能ごとに独立した小さなサービスに分割して構築する方式(マイクロサービス)の違いの説明として適切なものはどれか。","choices":[{"label":"ア","text":"マイクロサービスは常にモノリシックより単純に運用できる"},{"label":"イ","text":"両者に運用上の違いは一切ない"},{"label":"ウ","text":"マイクロサービスは各サービスを独立してデプロイ・スケールできる一方、サービス間通信の複雑さが増す"},{"label":"エ","text":"モノリシックは機能ごとに個別にデプロイできる"}],"correct":"ウ","hint":"マイクロサービスは開発・デプロイの独立性を高める一方、サービス間の通信・整合性管理という新たな課題も生む。"},{"id":94,"cat":"ソフトウェア・HI","topic":"RESTfulAPIの設計原則","q":"RESTfulなWeb APIの設計において、リソースの状態を取得する操作に一般的に用いられるHTTPメソッドはどれか。","choices":[{"label":"ア","text":"PUT(リソースの置き換え・更新に用いる)"},{"label":"イ","text":"GET"},{"label":"ウ","text":"POST(新規リソースの作成等に用いる)"},{"label":"エ","text":"DELETE(リソースの削除に用いる)"}],"correct":"イ","hint":"RESTではHTTPメソッドの意味に沿って操作を対応させ、GETは安全でべき等な参照操作として位置づけられる。"},{"id":95,"cat":"ソフトウェア・HI","topic":"プログレッシブエンハンスメントの考え方","q":"まず全ての利用者が利用できる最低限の基本機能を用意し、対応環境が整った利用者に対しては段階的に高度な機能を追加提供する設計思想を何と呼ぶか。","choices":[{"label":"ア","text":"プログレッシブエンハンスメント"},{"label":"イ","text":"フラットデザイン"},{"label":"ウ","text":"グレースフルデグラデーション(高機能を前提に、非対応環境で機能を段階的に縮退させる逆方向の思想)"},{"label":"エ","text":"レスポンシブデザイン"}],"correct":"ア","hint":"古いブラウザや通信環境が制限された利用者にも最低限のアクセス性を保証しつつ、対応環境ではより豊かな体験を提供できる。"},{"id":96,"cat":"情報セキュリティ","topic":"公開鍵暗号と共通鍵暗号の違い","q":"暗号化と復号に異なる鍵を使う方式と、同じ鍵を使う方式の一般的な特徴の組合せとして適切なものはどれか。","choices":[{"label":"ア","text":"共通鍵暗号は鍵を公開しても安全である"},{"label":"イ","text":"両者に処理速度の違いは一切ない"},{"label":"ウ","text":"公開鍵暗号は鍵配送の安全性を確保しやすいが処理が重く、共通鍵暗号は処理が軽いが鍵の安全な共有が課題となる"},{"label":"エ","text":"公開鍵暗号の方が常に高速に処理できる"}],"correct":"ウ","hint":"実務では、共通鍵で本文を暗号化し、その鍵自体を公開鍵暗号で安全に配送するハイブリッド方式が広く使われる。"},{"id":97,"cat":"情報セキュリティ","topic":"デジタル署名の目的","q":"送信者が秘密鍵で署名を付与し、受信者が送信者の公開鍵でその署名を検証することで確認できることはどれか。","choices":[{"label":"ア","text":"データの改ざんの有無と、送信者本人が作成したことの真正性"},{"label":"イ","text":"データの内容が第三者から一切見えないこと(暗号化の役割であり異なる)"},{"label":"ウ","text":"通信の遅延時間"},{"label":"エ","text":"送信先のIPアドレスの正当性"}],"correct":"ア","hint":"デジタル署名は暗号化とは目的が異なり、主に完全性(改ざん検知)と否認防止を実現するための仕組みである。"},{"id":98,"cat":"情報セキュリティ","topic":"SQLインジェクションへの対策","q":"利用者からの入力値をそのままSQL文に連結して実行することで、想定外のSQLが実行されてしまう脆弱性(SQLインジェクション)への根本的な対策はどれか。","choices":[{"label":"ア","text":"SQL文をクライアント側だけで組み立てる"},{"label":"イ","text":"プレースホルダを用いたバインド機構(プリペアドステートメント)を使用する"},{"label":"ウ","text":"エラーメッセージの表示を一切禁止するだけで対応する"},{"label":"エ","text":"入力欄の文字数を制限するだけで対応する"}],"correct":"イ","hint":"バインド機構を用いると入力値がSQLの構文として解釈されなくなり、根本的にインジェクションを防止できる。"},{"id":99,"cat":"情報セキュリティ","topic":"多要素認証(MFA)の効果","q":"パスワード(知識情報)に加えて、スマートフォンアプリのワンタイムコード(所持情報)を組み合わせて認証する多要素認証が、パスワード単体の認証に比べて有効とされる理由はどれか。","choices":[{"label":"ア","text":"パスワードそのものの複雑さが自動的に向上するため"},{"label":"イ","text":"通信速度が向上するため"},{"label":"ウ","text":"入力の手間が一切増えないため"},{"label":"エ","text":"一方の要素が漏えいしても、他の要素も併せて突破しなければ不正ログインできないため"}],"correct":"エ","hint":"パスワードの漏えいだけでは不正ログインが成立しにくくなり、アカウント乗っ取りのリスクを大きく低減できる。"},{"id":100,"cat":"情報セキュリティ","topic":"VPNのトンネリングの仕組み","q":"インターネットのような公衆網の上に、あたかも専用線のような論理的な通信経路を構築し、その中を暗号化したデータが通過する仕組みを何と呼ぶか。","choices":[{"label":"ア","text":"ルーティング(経路選択そのものを指す一般的な概念)"},{"label":"イ","text":"ミラーリング"},{"label":"ウ","text":"ロードバランシング"},{"label":"エ","text":"トンネリング"}],"correct":"エ","hint":"元のパケットを別のパケットでカプセル化して転送することで、公衆網上に仮想的な専用経路を作り出す。"}];

const C = {
  bg:"#0d1117", surface:"#161b22", surface2:"#1c2330", border:"#30363d",
  accent:"#58a6ff", green:"#3fb950", red:"#f85149", warn:"#d29922",
  text:"#e6edf3", muted:"#8b949e",
};

const s = {
  app:{ background:C.bg, minHeight:"100vh", padding:"20px 14px", fontFamily:"'Noto Sans JP',sans-serif", color:C.text },
  container:{ maxWidth:600, margin:"0 auto" },
  header:{ textAlign:"center", marginBottom:28 },
  h1:{ fontFamily:"monospace", fontSize:13, color:C.accent, letterSpacing:".1em", textTransform:"uppercase", marginBottom:4 },
  sub:{ color:C.muted, fontSize:12 },
  card:{ background:C.surface, border:`1px solid ${C.border}`, borderRadius:12, padding:20, marginBottom:14 },
  row:{ display:"flex", gap:8, flexWrap:"wrap", marginBottom:16 },
  chip:(a)=>({ padding:"5px 12px", background:a?"rgba(88,166,255,.1)":C.surface, border:`1px solid ${a?C.accent:C.border}`, borderRadius:20, color:a?C.accent:C.muted, fontSize:12, cursor:"pointer", fontFamily:"inherit" }),
  btn:(color=C.accent, outline=false)=>({ padding:"10px 20px", background:outline?"none":color, border:`1px solid ${color}`, borderRadius:8, color:outline?color:"#0d1117", fontFamily:"inherit", fontWeight:700, fontSize:14, cursor:"pointer" }),
  choiceBtn:(state)=>({
    width:"100%", padding:"11px 14px",
    background:state==="correct"?"rgba(63,185,80,.1)":state==="wrong"?"rgba(248,81,73,.1)":C.surface2,
    border:`1px solid ${state==="correct"?C.green:state==="wrong"?C.red:C.border}`,
    borderRadius:8, color:state==="correct"?C.green:state==="wrong"?C.red:C.text,
    fontFamily:"inherit", fontSize:16, textAlign:"left", cursor:state?"default":"pointer",
    display:"flex", gap:10, alignItems:"flex-start", lineHeight:1.5, marginBottom:7,
  }),
  label:{ fontFamily:"monospace", fontSize:14, color:C.muted, flexShrink:0, paddingTop:1 },
  fb:(ok)=>({ marginTop:14, padding:"12px 14px", borderRadius:8, fontSize:15, lineHeight:1.7, background:ok?"rgba(63,185,80,.08)":"rgba(248,81,73,.08)", border:`1px solid ${ok?"rgba(63,185,80,.3)":"rgba(248,81,73,.3)"}`, color:ok?C.green:C.red }),
  hintBox:{ marginTop:10, padding:"10px 12px", background:"rgba(255,255,255,.03)", border:`1px solid ${C.border}`, borderRadius:6, fontSize:14, color:C.muted, lineHeight:1.6 },
  hintLabel:{ fontSize:11, fontFamily:"monospace", color:C.accent, textTransform:"uppercase", letterSpacing:".05em", marginBottom:3 },
  progress:{ height:3, background:C.border, borderRadius:2, marginBottom:16, overflow:"hidden" },
  bar:(pct)=>({ height:"100%", background:C.accent, borderRadius:2, width:`${pct}%`, transition:"width .3s" }),
  meta:{ display:"flex", justifyContent:"space-between", alignItems:"center", marginBottom:12 },
  catTag:{ fontSize:11, padding:"2px 7px", background:"rgba(88,166,255,.1)", color:C.accent, borderRadius:4, fontFamily:"monospace" },
  qnum:{ fontSize:13, color:C.muted, fontFamily:"monospace", marginBottom:6 },
  qtext:{ fontSize:17, fontWeight:500, lineHeight:1.65, marginBottom:18 },
  tabs:{ display:"flex", gap:4, marginBottom:20, background:C.surface, padding:4, borderRadius:8, border:`1px solid ${C.border}` },
  tabBtn:(a)=>({ flex:1, padding:8, background:a?C.surface2:"none", border:"none", color:a?C.text:C.muted, fontFamily:"inherit", fontSize:15, borderRadius:6, cursor:"pointer", fontWeight:a?700:400 }),
  reviewItem:{ background:C.surface, border:`1px solid ${C.border}`, borderLeft:`3px solid ${C.red}`, borderRadius:8, padding:14, marginBottom:10 },
  sectionTitle:{ fontSize:11, color:C.muted, fontFamily:"monospace", textTransform:"uppercase", letterSpacing:".06em", margin:"20px 0 8px" },
  catBarRow:{ display:"flex", alignItems:"center", gap:8, marginBottom:8, background:C.surface, border:`1px solid ${C.border}`, borderRadius:8, padding:"9px 11px" },
  statBox:{ background:C.surface, border:`1px solid ${C.border}`, borderRadius:10, padding:"12px 8px", textAlign:"center", flex:1 },
  statNum:{ fontFamily:"monospace", fontSize:22, fontWeight:600, color:C.accent },
  statLabel:{ fontSize:11, color:C.muted, marginTop:2 },
  analysisBox:{ background:C.surface2, border:`1px solid ${C.border}`, borderRadius:10, padding:16, fontSize:13, lineHeight:1.8, color:C.text, whiteSpace:"pre-wrap" },
  spinner:{ display:"inline-block", width:16, height:16, border:`2px solid ${C.border}`, borderTop:`2px solid ${C.accent}`, borderRadius:"50%", animation:"spin 0.8s linear infinite", verticalAlign:"middle" },
  errBox:{ padding:"12px 14px", background:"rgba(248,81,73,.08)", border:`1px solid rgba(248,81,73,.3)`, borderRadius:8, color:C.red, fontSize:13, marginTop:12 },
  copyBox:{ background:C.surface2, border:`1px solid ${C.border}`, borderRadius:8, padding:12, fontSize:12, fontFamily:"monospace", color:C.muted, lineHeight:1.7, whiteSpace:"pre-wrap", wordBreak:"break-all", marginTop:8, maxHeight:160, overflowY:"auto" },
  copyBtn:(copied)=>({ padding:"8px 16px", background:copied?"rgba(63,185,80,.15)":"none", border:`1px solid ${copied?C.green:C.border}`, borderRadius:6, color:copied?C.green:C.muted, fontFamily:"inherit", fontSize:12, cursor:"pointer", transition:"all .2s" }),
  stockBadge:(n)=>({ display:"inline-block", padding:"2px 8px", borderRadius:10, fontSize:12, fontFamily:"monospace", fontWeight:700, background:n>=20?"rgba(63,185,80,.15)":n>0?"rgba(210,153,34,.15)":"rgba(248,81,73,.15)", color:n>=20?C.green:n>0?C.warn:C.red }),
};

function rateColor(p){ return p>=80?C.green:p>=50?C.warn:C.red; }
function shuffle(arr){ const a=[...arr]; for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]];} return a; }

async function callClaude(messages, system){
  const apiKey = process.env.REACT_APP_ANTHROPIC_KEY || "";
  const headers = {"Content-Type":"application/json"};
  if(apiKey) headers["x-api-key"] = apiKey;
  const res = await fetch("https://api.anthropic.com/v1/messages",{
    method:"POST", headers,
    body:JSON.stringify({ model:"claude-sonnet-4-6", max_tokens:1000, system, messages }),
  });
  const data = await res.json();
  return data.content?.map(b=>b.text||"").join("")||"";
}

async function analyzeResults(questions, answers, catStats){
  const log = questions.map((q,i)=>`Q${i+1}[${q.cat}/${q.topic}]: ${answers[i]===q.correct?"○":"✗"}`).join("\n");
  const stats = Object.entries(catStats).map(([c,s])=>`${c}: ${Math.round(s.ok/s.total*100)}% (${s.ok}/${s.total}問)`).join("\n");
  const system = `あなたは基本情報技術者試験の学習コーチです。結果を分析し①弱点②強み・弱みの評価③次の学習ステップを250〜350字で返してください。`;
  return await callClaude([{role:"user",content:`【回答】\n${log}\n\n【累計】\n${stats}`}], system);
}

function ReviewCopyBox({ missedList, lifetimeByCat }){
  const [copied, setCopied] = useState(false);

  const buildText = useCallback(()=>{
    const now = new Date();
    const ds = `${now.getFullYear()}/${now.getMonth()+1}/${now.getDate()} ${String(now.getHours()).padStart(2,"0")}:${String(now.getMinutes()).padStart(2,"0")}`;
    const catGroup = {};
    missedList.forEach(q=>{ if(!catGroup[q.cat]) catGroup[q.cat]=[]; catGroup[q.cat].push(q); });
    const lines = [
      `📝 FE科目A 復習リスト ${ds}`,
      `間違えた問題: ${missedList.length}問`,
      `---`,
    ];
    Object.entries(catGroup).forEach(([cat, qs])=>{
      // 分野別の正解率は「累計(生涯)」の実績を使う。今回の周回だけの一時的な
      // 集計だと、ページを開き直した直後は0%になってしまうため。
      const st = lifetimeByCat[cat];
      const pct = st && st.total>0 ? Math.round(st.ok/st.total*100) : null;
      const pctLabel = pct===null ? "累計データなし" : `累計正解率 ${pct}%`;
      lines.push(`\n■ ${cat}（${pctLabel}）`);
      qs.forEach((q,i)=>{
        const cc = q.choices.find(c=>c.label===q.correct);
        lines.push(`Q: ${q.q}`);
        lines.push(`A: ${q.correct}. ${cc?.text}`);
        lines.push(`解説: ${q.hint}`);
        if(i < qs.length-1) lines.push("");
      });
    });
    return lines.join("\n");
  },[missedList, lifetimeByCat]);

  const handleCopy = useCallback(()=>{
    const text = buildText();
    navigator.clipboard.writeText(text).then(()=>{
      setCopied(true); setTimeout(()=>setCopied(false), 2000);
    }).catch(()=>{
      const ta = document.getElementById("review-copy-area");
      if(ta){ ta.value=text; ta.select(); document.execCommand("copy"); setCopied(true); setTimeout(()=>setCopied(false),2000); }
    });
  },[buildText]);

  return(
    <div style={{background:C.surface2, border:`1px solid ${C.border}`, borderRadius:10, padding:14, marginBottom:16}}>
      <div style={{display:"flex", justifyContent:"space-between", alignItems:"center", marginBottom:8}}>
        <div style={{fontSize:12, color:C.muted}}>📋 復習リストをチャットに貼る</div>
        <button style={s.copyBtn(copied)} onClick={handleCopy}>
          {copied ? "✓ コピーしました" : "コピー"}
        </button>
      </div>
      <div style={{fontSize:11, color:C.muted, lineHeight:1.6}}>
        間違えた問題の正解・解説を分野別にまとめたテキストをコピーできます。<br/>
        コピー後このチャットに貼り付けると履歴として残ります。
      </div>
      <textarea id="review-copy-area" readOnly value={buildText()}
        style={{position:"absolute", left:"-9999px", top:0}}/>
    </div>
  );
}

export default function App(){
  const [tab, setTab] = useState("quiz");
  const [cat, setCat] = useState("すべて");
  const [weakMode, setWeakMode] = useState(false);
  const [weakIds, setWeakIds] = useState([]);
  const [phase, setPhase] = useState("idle");
  const [usedIds, setUsedIds] = useState([]);
  const [questions, setQuestions] = useState([]);
  const [answers, setAnswers] = useState([]);
  const [qIdx, setQIdx] = useState(0);
  const [chosen, setChosen] = useState(null);
  const [showFb, setShowFb] = useState(false);
  const [analysis, setAnalysis] = useState("");
  const [allHistory, setAllHistory] = useState([]);
  const [savedSessions, setSavedSessions] = useState([]);
  const [lifetime, setLifetime] = useState({totalAnswered:0,totalCorrect:0,byCat:{}});
  const [missedList, setMissedList] = useState([]);
  const [catStats, setCatStats] = useState({});
  const [isWeakSession, setIsWeakSession] = useState(false);
  const [copyText, setCopyText] = useState("");
  const [copied, setCopied] = useState(false);
  const [progressLoading, setProgressLoading] = useState(true);
  const [progressError, setProgressError] = useState("");

  // 起動時にlocalStorageから進捗・履歴を復元
  useEffect(()=>{
    // 問題内容が(id構成として)前回と変わっていないかを確認する。
    // 変わっていた場合、古い周回進捗・復習リストは今の問題内容と対応しなくなるため
    // 自動的にクリアする(生涯累計・セッション履歴は問題内容が変わっても意味を持つため維持する)。
    const currentSignature = ALL_QUESTIONS.length + ":" + ALL_QUESTIONS.map(q=>q.id).join(",");
    let storedSignature = null;
    try{ storedSignature = localStorage.getItem(LS_QVERSION); }catch(e){}
    const contentChanged = storedSignature !== null && storedSignature !== currentSignature;
    if(contentChanged){
      try{
        localStorage.removeItem(LS_USED);
        localStorage.removeItem(LS_CYCLE);
        localStorage.removeItem(LS_MISSED);
      }catch(e){}
    }
    try{ localStorage.setItem(LS_QVERSION, currentSignature); }catch(e){}

    const ids = contentChanged ? [] : store.loadIds().filter(id => ALL_QUESTIONS.some(q=>q.id===id));
    if(ids.length > 0){
      setUsedIds(ids);
      setProgressError(`✓ 今の周回で${ids.length}問に解答済み（残り${ALL_QUESTIONS.length-ids.length}問／全${ALL_QUESTIONS.length}問）`);
    } else if(contentChanged){
      setProgressError(`問題の内容が更新されました。周回・復習リストを初期化しました（累計成績は引き続き保持されています）。`);
    }
    // 過去のセッション履歴を復元(直近最大100件、表示専用)
    const saved = store.loadSessions();
    if(saved.length > 0) setSavedSessions(saved);
    // 生涯累計を復元(こちらは間引かれない真の累計。問題内容が変わっても維持する)
    setLifetime(store.loadLifetime());
    // 復習リストを復元
    if(!contentChanged){
      const missed = store.loadMissed();
      if(missed.length > 0) setMissedList(missed);
    }
    // 今回の周回成績を復元(リロードしても消えない。usedIdsが空＝新しい周回なら
    // 古いcycleデータが残っていても無視して空から始める)
    if(ids.length > 0){
      const cyc = store.loadCycle();
      if(cyc.history && cyc.history.length > 0) setAllHistory(cyc.history);
      if(cyc.catStats) setCatStats(cyc.catStats);
    } else {
      store.clearCycle();
    }
    setProgressLoading(false);
  },[]); // eslint-disable-line

  // 残り問題数
  const available = ALL_QUESTIONS.filter(q=>{
    const catOk = cat==="すべて" || q.cat===cat;
    const notUsed = !usedIds.includes(q.id);
    return catOk && notUsed;
  });

  const saveUsedIds = useCallback((ids)=>{
    setUsedIds(ids);
    store.saveIds(ids);
  },[]);

  const startSession = useCallback(async()=>{
    let picked = [];
    const usingWeak = weakMode && weakIds.length > 0;
    setIsWeakSession(usingWeak);

    if(usingWeak){
      // 苦手優先：間違えた問題IDから優先出題
      const weakInAll = ALL_QUESTIONS.filter(q => weakIds.includes(q.id));
      const others = ALL_QUESTIONS.filter(q => !weakIds.includes(q.id) && (cat==="すべて" || q.cat===cat));
      const shuffledWeak = shuffle(weakInAll).slice(0, Math.min(10, weakInAll.length));
      const rest = shuffle(others).slice(0, Math.max(0, 10 - shuffledWeak.length));
      picked = shuffle([...shuffledWeak, ...rest]).slice(0, 10);
    } else {
      const base = ALL_QUESTIONS.filter(q=>(cat==="すべて"||q.cat===cat) && !usedIds.includes(q.id));
      if(base.length === 0){
        const fresh = ALL_QUESTIONS.filter(q=>cat==="すべて"||q.cat===cat);
        picked = shuffle(fresh).slice(0,10);
        saveUsedIds(picked.map(q=>q.id));
        // 全問題を1周し終えて新しい周回に入るため、今回の周回成績もリセットする
        setAllHistory([]); setCatStats({}); store.clearCycle();
      } else {
        // 残りが10問未満(周回の端数)の場合は、その残り分だけの少人数セッションにする。
        // 以前は残り10問未満で無条件に「周回終了」と誤判定し、周回の途中(例: 96問中90問時点)
        // でも今の周回の成績を強制リセットしてしまうバグがあった。
        const sessionSize = Math.min(10, base.length);
        picked = shuffle(base).slice(0, sessionSize);
        saveUsedIds([...usedIds, ...picked.map(q=>q.id)]);
      }
    }

    setQuestions(picked);
    setAnswers(new Array(picked.length).fill(null));
    setQIdx(0); setChosen(null); setShowFb(false);
    setAnalysis(""); setCopyText(""); setCopied(false);
    setPhase("question");
  },[cat, usedIds, weakMode, weakIds, saveUsedIds]);

  const handleAnswer = useCallback((choice)=>{
    if(showFb) return;
    setChosen(choice); setShowFb(true);
    const q = questions[qIdx];
    const ok = choice.label===q.correct;
    // 苦手優先モードは既出問題を意図的に何度も再出題する復習用モードのため、
    // 「今の周回(1周=全問題を重複なく1回ずつ)」の集計(allHistory/catStats)には含めない。
    // 含めてしまうと同じ問題が何度もカウントされ、分野の合計がその分野の
    // 実際の問題数を超えるなど、集計が壊れる原因になっていた。
    if(!isWeakSession){
      setAllHistory(h=>{
        const updatedHistory=[...h,{cat:q.cat,topic:q.topic,correct:ok}];
        setCatStats(prev=>{
          const cur=prev[q.cat]||{ok:0,total:0};
          const updatedStats={...prev,[q.cat]:{ok:cur.ok+(ok?1:0),total:cur.total+1}};
          store.saveCycle({history:updatedHistory, catStats:updatedStats});
          return updatedStats;
        });
        return updatedHistory;
      });
    }
    if(!ok){
      setMissedList(m=>{
        const updated=[...m,q];
        store.saveMissed(updated);
        return updated;
      });
    }
    setAnswers(prev=>{const n=[...prev];n[qIdx]=choice.label;return n;});
  },[showFb,questions,qIdx,isWeakSession]);

  const handleNext = useCallback(()=>{
    const next=qIdx+1;
    if(next>=questions.length) setPhase("score");
    else{setQIdx(next);setChosen(null);setShowFb(false);}
  },[qIdx,questions.length]);

  const sessionCorrect = answers.filter((a,i)=>a!==null&&questions[i]&&a===questions[i].correct).length;


  useEffect(()=>{
    if(phase==="score"&&questions.length>0){
      if(copyText===""){
        const now=new Date();
        const ds=`${now.getFullYear()}/${now.getMonth()+1}/${now.getDate()} ${String(now.getHours()).padStart(2,"0")}:${String(now.getMinutes()).padStart(2,"0")}`;
        const correct=answers.filter((a,i)=>a!==null&&questions[i]&&a===questions[i].correct).length;
        const lines=[
          `📊 FE科目A クイズ結果 ${ds}`,
          `正解: ${correct}/${questions.length}問 (${Math.round(correct/questions.length*100)}%)`,
          `分野: ${[...new Set(questions.map(q=>q.cat))].join("・")}`,
          `---`,
          ...questions.map((q,i)=>`Q${i+1}[${q.cat}/${q.topic}] ${answers[i]===q.correct?"○":"✗"}`),
          `---`,
          `累計分野別:`,
          ...Object.entries(catStats).map(([c,s])=>`${c}: ${Math.round(s.ok/s.total*100)}% (${s.ok}/${s.total})`),
        ];
        setCopyText(lines.join("\n"));

        // このセッションの分野別集計を作りlocalStorageへ永続化する
        const sessionCats = {};
        questions.forEach((q,i)=>{
          if(!sessionCats[q.cat]) sessionCats[q.cat]={ok:0,total:0};
          sessionCats[q.cat].total+=1;
          if(answers[i]!==null && answers[i]===q.correct) sessionCats[q.cat].ok+=1;
        });
        const session={
          date: now.getTime(),
          cat: cat,
          total: questions.length,
          correct: correct,
          pct: Math.round(correct/questions.length*100),
          cats: sessionCats,
        };
        store.addSession(session);
        setSavedSessions(prev=>{
          const updated=[...prev, session];
          return updated.length>100 ? updated.slice(updated.length-100) : updated;
        });
        setLifetime(store.loadLifetime());
      }
    }
  },[phase]); // eslint-disable-line

  const loadDbHistory = useCallback(()=>{ /* localStorage版は履歴タブで直接表示 */ },[]);

  const clearDbMissed = useCallback(async()=>{
    if(!window.confirm("復習リストを全削除しますか？")) return;
    setDbMissed([]);
  },[]);

  const handleAnalyze = useCallback(async()=>{
    setPhase("analyzing");
    try{
      const result=await analyzeResults(questions,answers,catStats);
      setAnalysis(result);
      setCopyText(prev=>prev+"\n---\n📝 AI分析:\n"+result);
    }catch{setAnalysis("分析の取得に失敗しました。");}
    finally{setPhase("score");}
  },[questions,answers,catStats]);

  const resetSession = useCallback(()=>{setPhase("idle");setAnalysis("");setCopyText("");setCopied(false);},[]);
  const clearMissed = useCallback(()=>{ setMissedList([]); store.saveMissed([]); },[]);

  // 「今の周回」の集計はallHistory(今の周回で回答した{cat,correct}の記録)から
  // 毎回計算し直す。allHistoryはusedIdsと必ず同じタイミングでリセットされるため
  // (store.clearCycle()を常に一緒に呼んでいる)、この2つの間にズレは生じない。
  // ※missedList(復習リスト)は周回をまたいで蓄積し続ける別物であり、
  // 「今の周回」の集計には使えない(前回、誤って使ってしまい負の値になるバグを出した)。
  const cycleStats = (()=>{
    const byCat = {};
    allHistory.forEach(h=>{
      if(!byCat[h.cat]) byCat[h.cat] = { ok:0, total:0 };
      byCat[h.cat].total += 1;
      if(h.correct) byCat[h.cat].ok += 1;
    });
    const totalAnswered = allHistory.length;
    const totalCorrect = allHistory.filter(h=>h.correct).length;
    return { byCat, totalAnswered, totalCorrect };
  })();
  const totalAnswered = cycleStats.totalAnswered;
  const totalCorrect = cycleStats.totalCorrect;
  const overallPct=totalAnswered>0?Math.round(totalCorrect/totalAnswered*100):0;

  return(
    <div style={s.app}>
      <style>{`@keyframes spin{to{transform:rotate(360deg)}} @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700&display=swap');`}</style>
      <div style={s.container}>
        <header style={s.header}>
          <div style={s.h1}>FE 科目A Quiz</div>
          <div style={s.sub}>基本情報技術者 — 100問内蔵</div>
          <a href="/b" style={{display:"inline-block",marginTop:8,fontSize:12,color:C.accent,textDecoration:"none",border:`1px solid ${C.accent}`,borderRadius:6,padding:"4px 10px"}}>科目Bの問題を解く →</a>
        </header>

        <div style={s.tabs}>
          {["quiz","review","history"].map((t,i)=>(
            <button key={t} style={s.tabBtn(tab===t)} onClick={()=>{ setTab(t); if(t==="history") loadDbHistory(); }}>
              {["クイズ",`復習${missedList.length>0?` (${missedList.length})`:""}`, "履歴・分析"][i]}
            </button>
          ))}
        </div>

        {tab==="quiz" && <>
          {phase==="idle" && (
            <div style={s.card}>
              {progressLoading ? (
                <div style={{textAlign:"center",padding:20,color:C.muted,fontSize:13}}>
                  <span style={s.spinner}/> <span style={{marginLeft:8}}>進捗を読み込み中…</span>
                </div>
              ) : (
                <div style={{marginBottom:12,fontSize:13,color:C.muted}}>
                  残り問題: <span style={s.stockBadge(available.length)}>{available.length}問</span>
                  <span style={{fontSize:11,color:C.muted,marginLeft:8}}>/ {ALL_QUESTIONS.length}問中</span>
                  {progressError && <div style={{fontSize:11,color:progressError.startsWith("✓")?C.green:C.warn,marginTop:4}}>{progressError}</div>}
                </div>
              )}
              <div style={{marginBottom:16}}>
                <div style={{fontSize:11,color:C.muted,fontFamily:"monospace",marginBottom:8,textTransform:"uppercase",letterSpacing:".06em"}}>分野</div>
                <div style={s.row}>
                  {CATS.map(c=><button key={c} style={s.chip(cat===c)} onClick={()=>setCat(c)}>{c}</button>)}
                </div>
              </div>
              <button style={{...s.btn(),width:"100%"}} onClick={startSession}>
                {weakMode ? "🎯 苦手優先モードでスタート" : `スタート（${Math.min(10, available.length===0?10:available.length)}問）`}
              </button>
              {/* 苦手優先モード切替 */}
              <div style={{display:"flex",alignItems:"center",gap:10,marginTop:10,padding:"10px 12px",background:weakMode?"rgba(63,185,80,.08)":"rgba(255,255,255,.03)",border:`1px solid ${weakMode?C.green:C.border}`,borderRadius:8,cursor:"pointer"}}
                onClick={async()=>{
                  const next = !weakMode;
                  setWeakMode(next);
                  if(next){
                    // localStorageの復習リストから苦手IDを取得
                    const ids = missedList.map(q=>q.id);
                    setWeakIds([...new Set(ids)]);
                  }
                }}>
                <div style={{width:36,height:20,background:weakMode?C.green:C.border,borderRadius:10,position:"relative",flexShrink:0,transition:"background .2s"}}>
                  <div style={{width:16,height:16,background:"white",borderRadius:"50%",position:"absolute",top:2,left:weakMode?18:2,transition:"left .2s"}}/>
                </div>
                <div>
                  <div style={{fontSize:13,color:weakMode?C.green:C.muted,fontWeight:weakMode?700:400}}>苦手優先モード</div>
                  <div style={{fontSize:11,color:C.muted}}>{weakMode && weakIds.length>0 ? `${weakIds.length}問を優先出題` : "復習リストの問題を優先"}</div>
                </div>
              </div>
              <button style={{width:"100%",padding:9,background:"none",border:`1px solid ${C.border}`,color:C.muted,borderRadius:8,fontFamily:"inherit",fontSize:12,cursor:"pointer",marginTop:8}}
                onClick={()=>{ if(window.confirm("使用済み問題をリセットして全問を出題可能にします。今回の周回成績もリセットされます。よろしいですか？")){ saveUsedIds([]); setAllHistory([]); setCatStats({}); store.clearCycle(); } }}>
                🔄 問題をリセット（全100問に戻す）
              </button>
              <button style={{width:"100%",padding:9,background:"none",border:`1px solid #7f1d1d`,color:"#f87171",borderRadius:8,fontFamily:"inherit",fontSize:12,cursor:"pointer",marginTop:8}}
                onClick={()=>{
                  if(!window.confirm("累計成績・履歴・復習リストを含む全てのデータを完全に削除します。これまでの学習記録は元に戻せません。本当によろしいですか？")) return;
                  if(!window.confirm("最終確認です。累計解答数・正解率など、これまでの記録は全て消えます。本当に実行しますか？")) return;
                  try{
                    localStorage.removeItem(LS_USED);
                    localStorage.removeItem(LS_SESSIONS);
                    localStorage.removeItem(LS_MISSED);
                    localStorage.removeItem(LS_LIFETIME);
                    localStorage.removeItem(LS_CYCLE);
                    localStorage.removeItem(LS_QVERSION);
                  }catch(e){}
                  window.location.reload();
                }}>
                🗑️ 完全リセット（累計成績も含めて全データ削除）
              </button>
            </div>
          )}

          {phase==="question" && questions[qIdx] && (()=>{
            const q=questions[qIdx];
            return(
              <div>
                <div style={s.progress}><div style={s.bar((qIdx/questions.length)*100)}/></div>
                <div style={s.card}>
                  <div style={s.meta}>
                    <span style={{fontFamily:"monospace",fontSize:12,color:C.muted}}>{qIdx+1} / {questions.length}</span>
                    <span style={s.catTag}>{q.cat}</span>
                  </div>
                  <div style={s.qnum}>テーマ: {q.topic}</div>
                  <div style={s.qtext}>{q.q}</div>
                  <div>
                    {q.choices.map(c=>{
                      let state=null;
                      if(showFb){if(c.label===q.correct)state="correct";else if(chosen?.label===c.label)state="wrong";}
                      return(
                        <button key={c.label} style={s.choiceBtn(state)} onClick={()=>handleAnswer(c)} disabled={showFb}>
                          <span style={s.label}>{c.label}</span><span>{c.text}</span>
                        </button>
                      );
                    })}
                  </div>
                  {showFb && (
                    <div>
                      <div style={s.fb(chosen?.label===q.correct)}>
                        {chosen?.label===q.correct?"✓ 正解！":`✗ 不正解。正解は ${q.correct}. ${q.choices.find(c=>c.label===q.correct)?.text}`}
                        <div style={s.hintBox}><div style={s.hintLabel}>解説</div>{q.hint}</div>
                        {chosen?.label!==q.correct && (
                          <div style={{...s.hintBox,marginTop:8,borderColor:"rgba(248,81,73,.2)"}}>
                            <div style={{...s.hintLabel,color:C.warn}}>あなたの選択</div>
                            {chosen?.label}. {chosen?.text}
                          </div>
                        )}
                      </div>
                      <button style={{...s.btn(),width:"100%",marginTop:14}} onClick={handleNext}>
                        {qIdx+1>=questions.length?"結果を見る":"次の問題 →"}
                      </button>
                    </div>
                  )}
                </div>
              </div>
            );
          })()}

          {(phase==="score"||phase==="analyzing") && (
            <div style={{...s.card,textAlign:"center"}}>
              <div style={{fontFamily:"monospace",fontSize:48,fontWeight:600,color:C.accent,marginBottom:4}}>
                {sessionCorrect} / {questions.length}
              </div>
              <div style={{color:C.muted,fontSize:13,marginBottom:20}}>
                正解率 {Math.round(sessionCorrect/questions.length*100)}%
                {Math.round(sessionCorrect/questions.length*100)>=80?" — 完璧に近い！":Math.round(sessionCorrect/questions.length*100)>=60?" — 惜しい、復習しよう":" — 復習タブで確認を"}
              </div>
              <div style={{textAlign:"left",marginBottom:20}}>
                {questions.map((q,i)=>{
                  const ok=answers[i]===q.correct;
                  return(
                    <div key={i} style={{display:"flex",justifyContent:"space-between",alignItems:"flex-start",padding:"7px 0",borderBottom:`1px solid ${C.border}`,gap:8}}>
                      <div style={{fontSize:12}}>
                        <span style={{fontFamily:"monospace",color:C.muted,marginRight:6}}>Q{i+1}</span>
                        <span style={{color:C.muted,fontSize:11}}>[{q.cat}]</span>
                        <span style={{marginLeft:6}}>{q.topic}</span>
                      </div>
                      <span style={{fontFamily:"monospace",color:ok?C.green:C.red,flexShrink:0}}>{ok?"○":"✗"}</span>
                    </div>
                  );
                })}
              </div>
              <div style={{display:"flex",gap:8,justifyContent:"center",flexWrap:"wrap",marginBottom:16}}>
                <button style={s.btn()} onClick={startSession}>もう一度</button>
                <button style={s.btn(C.accent,true)} onClick={resetSession}>設定に戻る</button>
              </div>
              <div style={{textAlign:"left"}}>
                <div style={s.sectionTitle}>AI 学習分析</div>
                {analysis?(
                  <div style={s.analysisBox}>{analysis}</div>
                ):(
                  <button style={{...s.btn(C.green),width:"100%"}} onClick={handleAnalyze} disabled={phase==="analyzing"}>
                    {phase==="analyzing"?<><span style={s.spinner}/><span style={{marginLeft:8}}>分析中…</span></>:"このセッションをAIに分析してもらう"}
                  </button>
                )}
              </div>
              {copyText && (
                <div style={{textAlign:"left",marginTop:20}}>
                  <div style={s.sectionTitle}>📋 チャットに貼り付ける用</div>
                  <div style={{display:"flex",justifyContent:"flex-end",marginBottom:4}}>
                    <button style={s.copyBtn(copied)} onClick={()=>{
                      navigator.clipboard.writeText(copyText).then(()=>{setCopied(true);setTimeout(()=>setCopied(false),2000);})
                      .catch(()=>{const el=document.getElementById("copyarea");if(el){el.select();document.execCommand("copy");setCopied(true);setTimeout(()=>setCopied(false),2000);}});
                    }}>{copied?"✓ コピーしました":"コピー"}</button>
                  </div>
                  <textarea id="copyarea" readOnly value={copyText}
                    style={{...s.copyBox,width:"100%",resize:"none",outline:"none"}} rows={8}/>
                  <div style={{fontSize:11,color:C.muted,marginTop:4}}>コピーしてチャットに貼り付けると履歴として残ります</div>
                </div>
              )}
            </div>
          )}
        </>}

        {tab==="review" && (
          <div>
            {missedList.length===0
              ?<div style={{textAlign:"center",color:C.muted,fontSize:14,padding:"48px 0"}}>間違えた問題はまだありません。</div>
              :<>
                {/* コピーテキスト生成 */}
                <ReviewCopyBox missedList={missedList} lifetimeByCat={lifetime.byCat||{}}/>
                <button style={{width:"100%",padding:9,background:"none",border:`1px solid ${C.border}`,color:C.muted,borderRadius:8,fontFamily:"inherit",fontSize:13,cursor:"pointer",marginBottom:16}} onClick={clearMissed}>復習リストをクリア</button>
                {missedList.map((q,i)=>{
                  const cc=q.choices.find(c=>c.label===q.correct);
                  return(
                    <div key={i} style={s.reviewItem}>
                      <div style={{fontSize:11,color:C.muted,fontFamily:"monospace",marginBottom:4}}>{q.cat} / {q.topic}</div>
                      <div style={{fontSize:13,fontWeight:500,lineHeight:1.6,marginBottom:8}}>{q.q}</div>
                      <div style={s.hintLabel}>正解</div>
                      <div style={{fontSize:13,color:C.text,marginBottom:6,lineHeight:1.5}}>{q.correct}. {cc?.text}</div>
                      <div style={{fontSize:12,color:C.muted,lineHeight:1.6}}>{q.hint}</div>
                    </div>
                  );
                })}
              </>
            }
          </div>
        )}

        {tab==="history" && (
          <div>
            <div style={{display:"flex",gap:8,marginBottom:18}}>
              {[["解答数",totalAnswered],["正解数",totalCorrect],["正解率",`${overallPct}%`]].map(([l,v],i)=>(
                <div key={l} style={s.statBox}>
                  <div style={{...s.statNum,color:i===2?rateColor(overallPct):C.accent}}>{v}</div>
                  <div style={s.statLabel}>{l}（今の周回）</div>
                </div>
              ))}
            </div>
            {Object.keys(cycleStats.byCat).length>0 && <>
              <div style={s.sectionTitle}>分野別 正解率（今の周回・{ALL_QUESTIONS.length}問を1周する間ずっと蓄積）</div>
              {Object.entries(cycleStats.byCat)
                .map(([c,st])=>({c,pct:Math.round(st.ok/st.total*100),ok:st.ok,total:st.total}))
                .sort((a,b)=>a.pct-b.pct)
                .map(({c,pct,ok,total})=>(
                  <div key={c} style={s.catBarRow}>
                    <div style={{fontSize:12,width:140,flexShrink:0,lineHeight:1.3}}>{c}</div>
                    <div style={{flex:1,height:7,background:C.surface2,borderRadius:4,overflow:"hidden"}}>
                      <div style={{height:"100%",borderRadius:4,width:`${pct}%`,background:rateColor(pct),transition:"width .4s"}}/>
                    </div>
                    <div style={{fontFamily:"monospace",fontSize:12,width:72,textAlign:"right",color:rateColor(pct)}}>
                      {pct}% ({ok}/{total})
                    </div>
                  </div>
                ))
              }
            </>}

            {/* 累計成績(fe_lifetime基準、間引かれない真の累計)＋直近セッション一覧(表示専用) */}
            {(lifetime.totalAnswered > 0 || savedSessions.length > 0) && (()=>{
              const totalA = lifetime.totalAnswered;
              const totalC = lifetime.totalCorrect;
              const ovPct = totalA>0?Math.round(totalC/totalA*100):0;
              const aggCats = lifetime.byCat || {};
              return <>
                <div style={s.sectionTitle}>📦 累計（全セッション）</div>
                <div style={{display:"flex",gap:8,marginBottom:12}}>
                  {[["累計解答",totalA],["累計正解",totalC],["累計正解率",`${ovPct}%`]].map(([l,v],i)=>(
                    <div key={l} style={s.statBox}>
                      <div style={{...s.statNum,color:i===2?rateColor(ovPct):C.accent,fontSize:16}}>{v}</div>
                      <div style={s.statLabel}>{l}</div>
                    </div>
                  ))}
                </div>
                {Object.keys(aggCats).length>0 && <>
                  <div style={s.sectionTitle}>累計分野別正解率</div>
                  {Object.entries(aggCats)
                    .map(([c,st])=>({c,pct:Math.round(st.ok/st.total*100),ok:st.ok,total:st.total, legacy: !CATS.includes(c)}))
                    .sort((a,b)=>a.pct-b.pct)
                    .map(({c,pct,ok,total,legacy})=>(
                      <div key={c} style={s.catBarRow}>
                        <div style={{fontSize:12,width:140,flexShrink:0,lineHeight:1.3,color:legacy?C.muted:undefined}}>{c}{legacy?"（分野再編前の記録）":""}</div>
                        <div style={{flex:1,height:7,background:C.surface2,borderRadius:4,overflow:"hidden"}}>
                          <div style={{height:"100%",borderRadius:4,width:`${pct}%`,background:rateColor(pct),transition:"width .4s"}}/>
                        </div>
                        <div style={{fontFamily:"monospace",fontSize:12,width:72,textAlign:"right",color:rateColor(pct)}}>{pct}% ({ok}/{total})</div>
                      </div>
                    ))
                  }
                </>}
                <div style={s.sectionTitle}>直近のセッション（最新20件）</div>
                <div style={{background:C.surface,border:`1px solid ${C.border}`,borderRadius:8,padding:"4px 13px",marginBottom:12}}>
                  {savedSessions.slice(-20).reverse().map((sess,i)=>{
                    const d=new Date(sess.date);
                    const ds=`${d.getMonth()+1}/${d.getDate()} ${String(d.getHours()).padStart(2,"0")}:${String(d.getMinutes()).padStart(2,"0")}`;
                    return(
                      <div key={i} style={{display:"flex",justifyContent:"space-between",alignItems:"center",padding:"8px 0",borderBottom:i<Math.min(savedSessions.length,20)-1?`1px solid ${C.border}`:"none"}}>
                        <span style={{fontSize:12,color:C.muted,fontFamily:"monospace"}}>{ds} ・ {sess.cat}</span>
                        <span style={{fontFamily:"monospace",fontSize:13,fontWeight:600,color:rateColor(sess.pct)}}>{sess.correct}/{sess.total}</span>
                      </div>
                    );
                  })}
                </div>
                <button style={{width:"100%",padding:9,background:"none",border:`1px solid ${C.border}`,color:C.muted,borderRadius:8,fontFamily:"inherit",fontSize:12,cursor:"pointer"}}
                  onClick={()=>{ if(window.confirm("直近のセッション一覧のみ削除します(累計成績は消えません)。よろしいですか？")){ localStorage.removeItem("fe_sessions"); setSavedSessions([]); } }}>
                  直近セッション一覧をクリア
                </button>
              </>;
            })()}
            {totalAnswered===0 && savedSessions.length===0 && <div style={{textAlign:"center",color:C.muted,fontSize:13,padding:"12px 0"}}>クイズに挑戦すると履歴が表示されます。</div>}
          </div>
        )}
      </div>
    </div>
  );
}
